"""System Design knowledge bank — structured, broad-scope reference.

Powers the in-app "System Design" track on the Interview Prep page and
mirrors SYSTEM_DESIGN_ECOMMERCE_BANK.md. Each entry carries the full
schema: problem it solves, answer/how it works, real example, use cases,
architecture (ASCII), disadvantages, competing technologies.

Static reference content; per-user edits live in system_design_overrides
(see MIGRATION_SYSTEM_DESIGN_OVERRIDES.sql) and are merged at serve time.
"""

import re

CATEGORIES = {
    "core": "Core Building Blocks",
    "data_stores": "Databases & Data Stores",
    "data_platforms": "Data Platforms at Scale",
    "data_analytics": "Data Analytics",
    "data_modeling": "Data Modeling",
    "classic": "Classic Design Problems (Alex Xu style)",
    "ai_solutions": "AI Solutions",
    "infosec": "Information Security",
    "ecommerce": "E-commerce Scenarios",
}

# A broad tag per category, added automatically so category-level browsing
# and free-text tag search both work.
_CATEGORY_TAGS = {
    "core": ["building-block"],
    "data_stores": ["database"],
    "data_platforms": ["data-platform"],
    "data_analytics": ["analytics"],
    "data_modeling": ["data-modeling"],
    "classic": ["classic", "alex-xu"],
    "ai_solutions": ["ai", "ml"],
    "infosec": ["security"],
    "ecommerce": ["ecommerce"],
}

_TAG_STOP = {"the", "a", "an", "and", "or", "vs", "of", "to", "for", "in", "on",
             "at", "with", "avoid", "real", "time", "amp"}


# The full interview-framework walkthrough (Alex-Xu style). Entries may
# carry a `detail` dict keyed by these; rendered in order. is_mono marks
# ASCII-diagram sections (monospace).
DETAIL_ORDER = [
    ("scope", "1 · Understand the problem & scope", False),
    ("functional", "2 · Functional requirements", False),
    ("nonfunctional", "3 · Non-functional requirements", False),
    ("estimation", "4 · Back-of-the-envelope estimation", False),
    ("api", "5 · API design", False),
    ("data_model", "6 · Data model", False),
    ("hld", "7 · High-level design", False),
    ("hld_diagram", "High-level diagram", True),
    ("algorithms", "8 · Algorithms used", False),
    ("deep_dive", "9 · Design deep dive", False),
    ("follow_ups", "10 · Follow-up questions", False),
    ("final_diagram", "11 · Final design diagram", True),
    ("wrap_up", "12 · Wrap up", False),
]


def E(cat, title, problem, answer, example, use_cases, arch, disadv, competing, tags=None, detail=None):
    return {
        "cat": cat, "title": title, "problem": problem, "answer": answer,
        "example": example, "use_cases": use_cases, "arch": arch,
        "disadvantages": disadv, "competing": competing, "tags": tags,
        "detail": detail,
    }


# ─────────── Full framework walkthroughs (worked examples) ───────────

_DETAIL_URL = {
    "scope": "Shorten long URLs to short aliases and redirect back. Clarifying questions: traffic volume? alias length? custom aliases? link expiry? click analytics? read/write ratio? Assumptions: 100M new URLs/day, 100:1 read:write, 7-char alias, links live ~10 years, analytics optional.",
    "functional": "1) Given a long URL, return a shorter unique URL. 2) Given the short URL, redirect (HTTP 3xx) to the original. 3) Optional: custom alias, expiry date, click analytics.",
    "nonfunctional": "High availability (a broken redirect breaks every link ever shared). Low-latency redirects (<100 ms). Horizontally scalable to billions of URLs. Short keys should not be easily guessable/enumerable. Durable — no lost mappings.",
    "estimation": "Writes: 100M/day ÷ 86,400 ≈ 1,160 writes/s. Reads (100:1) ≈ 116,000 reads/s. Storage: 100M/day × 365 × 10y ≈ 365B records; ~500 B each ≈ ~180 TB. Key space: 62^7 ≈ 3.5 trillion — enough for 7 chars. Cache the hot 20% of links to absorb the read QPS.",
    "api": "POST /api/v1/data/shorten  body {longUrl}  ->  {shortUrl}.  GET /{shortKey}  ->  301/302 redirect to the long URL.  Optional body fields: customAlias, expireAt.",
    "data_model": "urls(short_key PK, long_url, user_id, created_at, expire_at). Access is a pure key lookup by short_key -> use a KV store (DynamoDB/Cassandra) or sharded SQL. Click events go to a separate analytics store (append-only), never the hot path.",
    "hld": "Write: client -> API -> unique-ID/key generator -> persist (short_key -> long_url) -> return short URL. Read: client -> API -> cache lookup -> DB on miss -> HTTP redirect. A CDN/edge can cache redirects for very hot links.",
    "hld_diagram": "Write:  Client -> API -> [ID gen -> base62] -> KV store\nRead:   Client -> API -> [Cache] --miss--> KV store -> 301 redirect",
    "algorithms": "Key generation: (A) Base62-encode a globally-unique ID (DB ticket-server ranges or Snowflake) — collision-free and short; add per-value salt/scramble so keys aren't sequentially guessable. (B) Hash the URL (MD5/SHA) and take the first 7 base62 chars — simple but needs collision handling (rehash/append). Prefer (A): no collisions, scales without coordination on the hot path.",
    "deep_dive": "Unique IDs at scale: a single auto-increment DB is a bottleneck — use a distributed ticket server (Zookeeper-assigned ranges) or Snowflake so each node mints IDs locally. 301 (permanent) is cached by browsers → fewer origin hits but no per-click analytics; 302 (temporary) hits the origin every time → enables analytics at higher load. Read path is cache-first (LRU) + CDN; shard the KV store by hash(short_key). Rate-limit creation to prevent abuse; scan new URLs against a safe-browsing list. Expiry via TTL + a lazy/batch cleanup job. Custom alias = conditional insert (unique constraint) to avoid races.",
    "follow_ups": "How do you add click analytics without slowing redirects? (emit an async event -> Kafka -> aggregation). How to block malicious URLs? (rate limit + URL reputation check). A single link goes viral — how to serve it? (CDN/edge cache + request coalescing). Support link deletion/expiry? (TTL). Custom domains and multi-region? (geo-DNS + regional replicas).",
    "final_diagram": "                         +-----------+\n Client --> API GW --> LB --> App nodes --> ID/key gen (Snowflake)\n                                  |             \n                    read: [Redis cache] --miss--> [KV store (sharded)]\n                                  |                       ^\n                          301/302 redirect               | write\n   click events -> [Kafka] -> [Aggregator] -> [Analytics store]",
    "wrap_up": "Core: base62 of a distributed unique ID for collision-free short keys; a sharded KV store as the system of record; a read-dominant path served by cache + CDN; the 301-vs-302 analytics trade-off; async click analytics off the hot path. Main bottlenecks — ID generation and read hotspots — are solved by a distributed ID scheme and aggressive caching.",
}

_DETAIL_CHAT = {
    "scope": "Design a 1:1 + group chat app (WhatsApp/Messenger). Clarifying questions: 1:1 only or groups? group size cap? text only or media? online presence? read receipts? end-to-end encryption? scale? Assumptions: 1:1 and small groups (≤100), text + media, presence + delivery/read receipts, 50M DAU, mobile-first.",
    "functional": "1) Send/receive messages in near real time (1:1 and group). 2) Message history & sync across devices. 3) Online/last-seen presence. 4) Delivery + read receipts. 5) Push notifications when offline. 6) Media attachments.",
    "nonfunctional": "Low latency delivery (<1 s). Highly available. Reliable delivery + correct ordering (no lost/duplicated/reordered messages). Scale to millions of concurrent connections. Consistency of message history; security (ideally E2E).",
    "estimation": "50M DAU, ~40 msgs/user/day = 2B msgs/day ≈ 23,000 msgs/s (peak higher). Concurrent connections: tens of millions of open WebSockets → many stateful chat servers behind a connection layer. Storage: 2B msgs/day × ~300 B ≈ 600 GB/day → sharded, time-partitioned message store.",
    "api": "WebSocket: connect (auth), sendMessage {chatId, content}, receive events. REST: GET /chats, GET /chats/{id}/messages?before=cursor (history, paginated), POST /chats (create), presence heartbeat. Push via APNs/FCM when the socket is closed.",
    "data_model": "messages(chat_id, message_id (Snowflake, time-sortable), sender_id, content, created_at) — partitioned by chat_id, ordered by message_id. chats/members(chat_id, user_id). A per-user 'sync/inbox' cursor for multi-device sync. Wide-column store (Cassandra) fits the write-heavy, per-chat, time-ordered access.",
    "hld": "Clients hold a persistent WebSocket to a chat server via a connection/load balancer. A session service maps user_id -> which chat server holds the socket. A message flows: sender -> chat server -> persist -> look up recipient's server -> push over their socket; if offline -> push notification + sync on reconnect. Presence via heartbeats in a fast store.",
    "hld_diagram": "Client A <=WS=> [Chat server 1] --+--> [Message store (Cassandra)]\n                                 |\n [Session/Presence (Redis)] <----+----> [Chat server 2] <=WS=> Client B\n                                 +--> offline? -> [Push (APNs/FCM)]",
    "algorithms": "Ordering: Snowflake/sequence IDs per chat give a total, time-sortable order (client sorts by id). Delivery: at-least-once + client-side dedup by message_id (exactly-once is impractical). Fan-out for groups: write the message once, fan out delivery to each online member's server; large groups use a pull/read-time model. Presence: heartbeat + TTL; publish changes via pub/sub.",
    "deep_dive": "Connection layer: millions of long-lived WebSockets are stateful — use many chat servers + a service-discovery/session store (user->server) so senders can route to a recipient's server; a message queue between servers decouples them. Offline delivery: persist first, then push; on reconnect the client syncs from its last cursor. Group scale: small groups fan-out-on-write; huge groups fan-out-on-read. Read receipts/typing: lightweight ephemeral events over pub/sub, not persisted as messages. E2E encryption (Signal protocol): server only routes ciphertext. Multi-device: per-device sync cursors + message copies.",
    "follow_ups": "How to guarantee ordering across devices? (per-chat sequence IDs). Handle a 100k-member group? (fan-out-on-read + rate limits). Media? (upload to object storage, send a reference, not bytes over the socket). Presence at scale without hammering the DB? (heartbeat + pub/sub, sampled). Exactly-once? (at-least-once + idempotent client dedup). End-to-end encryption trade-offs? (no server-side search/history).",
    "final_diagram": "  Clients <===WebSocket===> [ Load Balancer / Connection layer ]\n                                     |\n            +------------------------+------------------------+\n            v                        v                        v\n     [Chat server]            [Chat server]             [Chat server]\n            |   \\____ inter-server queue (route to recipient) ____/\n            v\n   [Message store: Cassandra]   [Presence/Session: Redis]   [Push: APNs/FCM]\n            ^\n   history/sync (cursor)  <--  REST API",
    "wrap_up": "Core: persistent WebSocket connection layer + stateful chat servers, a session store mapping user->server for routing, a time-partitioned wide-column message store, Snowflake IDs for ordering, at-least-once delivery with client dedup, offline push + cursor-based sync, and fan-out-on-write for small groups / read for large. Bottlenecks — connection state and group fan-out — are handled by horizontal chat servers, inter-server queues, and the hybrid fan-out model.",
}


def _title_tags(title):
    words = re.split(r"[^a-z0-9+]+", title.lower())
    return [w for w in words if w and w not in _TAG_STOP and len(w) > 2]


_DETAIL_UID = {
    "scope": "Generate globally-unique IDs across distributed servers. Clarify: numeric or alphanumeric? time-sortable required? scale (IDs/sec)? 64-bit fit? Assumptions: 64-bit numeric, roughly time-sortable, unique across data centers, 10k+ IDs/s/node, no central bottleneck.",
    "functional": "Generate unique 64-bit IDs; IDs increase roughly with time (k-sortable); high throughput; each node generates locally without coordination on the hot path.",
    "nonfunctional": "Absolute uniqueness (no collisions), high availability, very low latency (local generation), horizontal scalability, rough time ordering.",
    "estimation": "64-bit space ≈ 9.2×10^18 — ample. Snowflake layout: 41-bit ms timestamp = ~69 years; 10-bit node id = 1024 nodes; 12-bit sequence = 4096 IDs/ms/node ≈ 4M IDs/s/node.",
    "api": "Library call nextId() -> int64, or an ID service GET /id -> {id}. No per-ID network round trip in the library form.",
    "data_model": "Snowflake is stateless per node (nothing stored). Node-id assignment via Zookeeper/config. (Ticket-server alternative needs a single counter row -> bottleneck.)",
    "hld": "Each node composes an ID locally from [timestamp | node id | sequence]; no DB/network call per ID. Node ids assigned at startup (Zookeeper ephemeral nodes or static config). Clocks kept in sync via NTP.",
    "hld_diagram": "64-bit = [ 1 unused | 41 timestamp(ms) | 10 nodeId | 12 sequence ]\n each node builds IDs locally -> zero coordination on the hot path",
    "algorithms": "Snowflake: id = (ms << 22) | (nodeId << 12) | seq; seq resets each ms and spins to the next ms on overflow. Alternatives: UUIDv4 (128-bit random, not sortable), DB auto-increment (bottleneck), ticket server (SPOF), Flickr-style range hand-out.",
    "deep_dive": "Clock skew: if the clock moves backward, refuse or wait until it catches up — otherwise duplicates. Node-id uniqueness enforced by Zookeeper (ephemeral nodes) or careful config. Epoch choice sets the 69-year window. If guessability matters, scramble low bits (loses strict sortability). Sequence overflow (>4096/ms) waits for the next millisecond.",
    "follow_ups": "Two nodes with the same node id? (Zookeeper prevents it). Clock drift across DCs? (bounded by NTP; refuse backward time). Need strictly monotonic, not just roughly? (single generator or logical clock). Shorter/alphanumeric IDs? (base62-encode the 64-bit int).",
    "final_diagram": " Zookeeper -> assigns a unique nodeId to each generator node\n App -> local ID-gen node -> [timestamp | nodeId | seq] -> 64-bit id\n         (no network hop, ~4M ids/s/node)",
    "wrap_up": "Snowflake yields collision-free, time-sortable 64-bit IDs generated locally with no hot-path coordination. Main risks — clock skew and node-id uniqueness — are handled by NTP + refuse-backward and Zookeeper assignment. Trade-offs: sortable vs guessable, roughly vs strictly monotonic.",
}

_DETAIL_KV = {
    "scope": "Design a distributed key-value store (get/put) that stays available and scales horizontally. Clarify: consistency needs? value size? read/write ratio? durability? Assumptions: eventually consistent (AP) with tunable knobs, values <1MB, huge scale, low latency.",
    "functional": "put(key, value), get(key) -> value; scale to petabytes; always writable; tunable consistency.",
    "nonfunctional": "High availability (always writable), horizontal scalability, low latency, durability (no data loss), partition tolerance; consistency tunable per the CAP trade-off.",
    "estimation": "Target millions of ops/s across the fleet; replication factor N=3; each node owns partition ranges of the ring; storage sharded so adding nodes scales capacity and throughput linearly.",
    "api": "put(key, value) -> ack; get(key) -> (value, version). Internally: coordinator replicates to N nodes and applies quorum reads/writes.",
    "data_model": "Keys hashed onto a ring; each key lives on its N successor nodes. Value carries a vector clock/version for conflict resolution. LSM-tree storage (commit log + memtable -> SSTables + compaction) favors high write throughput.",
    "hld": "Consistent hashing places keys on a ring; any node can act as coordinator and forwards to the N replicas. Tunable quorum: W write acks + R read responses with W+R>N for read-your-writes-ish consistency. Gossip spreads membership/health; hinted handoff covers transient failures; Merkle-tree anti-entropy repairs divergent replicas.",
    "hld_diagram": "Client -> coordinator(any node) -> N replicas on the hash ring\n write: wait for W acks ; read: gather R responses ; W+R>N\n gossip(membership) + hinted handoff + Merkle-tree repair",
    "algorithms": "Consistent hashing (+ virtual nodes) for even spread and minimal remap on scaling. Quorum consensus (W+R>N) for tunable consistency. Vector clocks (or last-write-wins) to detect/resolve concurrent writes. Merkle trees to cheaply find divergent ranges during repair. Gossip for decentralized membership.",
    "deep_dive": "Consistency: W=1/R=1 = fast + eventual; W+R>N = stronger. Conflicts: vector clocks keep concurrent versions for the client to reconcile vs LWW (simpler, can silently drop writes). Failures: hinted handoff stashes writes for a down node and replays them; Merkle trees bound anti-entropy data transfer. Hotspots: virtual nodes balance load; hot keys still need caching. Durability: append to commit log then memtable, flush to immutable SSTables.",
    "follow_ups": "Add/remove nodes with no downtime? (consistent hashing moves only adjacent ranges). Need strong consistency? (raise quorum or run Paxos/Raft per shard). Very large values? (chunk + store references). Permanently dead node? (re-replicate its ranges). Multi-region? (per-DC replicas + async replication).",
    "final_diagram": "                [ hash ring: physical + virtual nodes ]\n Client -> coordinator -> { replica1, replica2, replica3 }\n            quorum W/R ; vector-clock versions\n gossip <-> all nodes ; hinted handoff ; Merkle anti-entropy",
    "wrap_up": "A Dynamo-style store: consistent hashing for partitioning, N-way replication with tunable quorums for the CAP trade-off, vector clocks for conflicts, and gossip + hinted handoff + Merkle trees for resilience and repair. It chooses availability + partition tolerance with tunable (usually eventual) consistency.",
}

_DETAIL_RL = {
    "scope": "Design a rate limiter capping requests per client. Clarify: client-side / server-side / gateway? per user / IP / API-key? limits (e.g. 100/min)? distributed? soft vs hard? Assumptions: server-side at the API gateway, per API-key, distributed across gateway nodes, hard limit, 429 on exceed.",
    "functional": "Allow N requests per window per client; reject excess with HTTP 429 + Retry-After; configurable rules per client/endpoint; accurate across a distributed fleet.",
    "nonfunctional": "Very low added latency, high availability, accuracy under concurrency, memory efficiency, and a defined fail-open vs fail-closed policy on limiter outage.",
    "estimation": "Hit on every request at high QPS -> counters in Redis (in-memory, atomic). Memory: token bucket = 1-2 numbers per client; sliding-window log = one entry per in-window request (heavier), so prefer bucket/counter variants at scale.",
    "api": "Transparent gateway middleware. Internally: atomic check-and-increment keyed by (client, window). Config API: POST /rules {clientId, limit, windowSec, scope}.",
    "data_model": "Redis keys: rate:{clientId}:{window} -> count (fixed/sliding window) OR a token bucket hash {tokens, lastRefillTs}; TTL = window length. Rules stored in config/DB and cached.",
    "hld": "Request -> gateway rate-limit middleware -> atomically check+update the client's counter in a shared Redis -> allow (forward to service) or reject (429). All gateway nodes share the same Redis so limits are global, not per-node.",
    "hld_diagram": "Client -> [Gateway node] -> Redis atomic INCR / Lua (client+window)\n           allow -> backend service ; over limit -> 429 + Retry-After",
    "algorithms": "Token bucket: tokens refill at a fixed rate up to a cap; each request spends one; allows bursts. Leaky bucket: constant drain, smooths bursts. Fixed window: simple counter (boundary-spike flaw). Sliding-window log: exact but memory-heavy. Sliding-window counter: weighted blend of current+previous window — good accuracy/memory balance. A Redis Lua script makes check-and-set atomic across nodes.",
    "deep_dive": "Distributed accuracy: a central Redis avoids per-node under-counting; Lua/atomic ops prevent races. Latency: keep it O(1) and colocate Redis; at extreme scale use local approximate counters synced periodically. Fixed-window boundary bursts are fixed by the sliding-window counter. Outage policy: fail-open (allow — favors availability) vs fail-closed (reject — favors protection). Emit 429 + Retry-After + X-RateLimit-* headers. Multiple rules (per-user AND per-IP AND per-endpoint) evaluated together.",
    "follow_ups": "Redis outage handling? (fail-open + local fallback). Combine per-user, per-IP, and global limits? (evaluate multiple buckets). Allow bursts? (token bucket). Exact fairness across nodes? (central store + atomic ops). Good client feedback? (429 + Retry-After + rate-limit headers).",
    "final_diagram": "Clients -> [LB] -> { Gateway nodes + rate-limit middleware }\n                        |  atomic check + increment (Lua)\n                        v\n                 [ Redis: client+window counters, TTL ]\n           allowed -> backend services ; exceeded -> 429",
    "wrap_up": "A gateway-level limiter using token bucket (bursty) or sliding-window counter (accurate), with counters in a shared Redis and atomic Lua for cross-node correctness. Key decisions: algorithm choice, central vs local counting, and fail-open vs fail-closed. Returns 429 + Retry-After.",
}

_DETAIL_FEED = {
    "scope": "Design a home news feed of ranked posts from followed accounts. Clarify: ranked or chronological? media? scale (DAU)? read/write ratio? Assumptions: ranked feed, 10M DAU, read-heavy, a following graph, text + media.",
    "functional": "1) Publish a post (fan out to followers). 2) Build a user's feed from accounts they follow. 3) Rank the feed. 4) Paginate / infinite scroll.",
    "nonfunctional": "Low feed-load latency (<200ms), high availability, scalable to huge follower counts, eventual consistency acceptable (a post can appear slightly late).",
    "estimation": "10M DAU × ~5 loads/day ≈ 50M reads/day ≈ 600/s (peak higher). ~2M posts/day. Fan-out: a post by someone with F followers writes F feed entries — celebrities blow this up.",
    "api": "POST /post {content}; GET /feed?cursor= -> ranked posts (paginated). Internals: a fan-out service + a per-user feed store.",
    "data_model": "posts(post_id Snowflake, user_id, content, created_at). follows(follower_id, followee_id). Per-user feed cache = list of post_ids (Redis). Posts in a KV/wide-column store; media in object storage.",
    "hld": "Write: user posts -> fan-out service finds followers -> pushes post_id into each follower's feed cache (fan-out-on-write). Read: load feed -> read post_ids from feed cache -> hydrate posts -> rank -> return. Hybrid: high-follower accounts use fan-out-on-read (pull) to avoid write explosions.",
    "hld_diagram": "Post -> [Fan-out service] -> follower feed caches (Redis lists)\n Load feed -> read feed cache -> hydrate posts -> rank -> page\n celebrity posts -> pulled at read time and merged",
    "algorithms": "Fan-out-on-write (push): precompute feeds — fast reads, heavy writes for popular users. Fan-out-on-read (pull): assemble at read — cheap writes, slower reads. Hybrid by follower threshold. Ranking: ML model over recency, affinity, and predicted engagement on a candidate set.",
    "deep_dive": "Celebrity problem: a 100M-follower post = 100M feed writes -> use pull for them and merge at read. Cap each cached feed (e.g., latest 1000 ids); older via pagination from source. Feeds are eventually consistent. Media stored in object storage + CDN; the feed holds references. Deletions/edits handled as tombstones at read. Ranking runs at read over the merged push+pull candidates.",
    "follow_ups": "Celebrity with 100M followers? (fan-out-on-read + merge). Chronological vs ranked toggle? Real-time updates? (WebSocket/poll). Repost/dedup handling? Brand-new user cold start? (popular/onboarding feed). Avoid showing the same post twice? (cursor + seen-set).",
    "final_diagram": " Publish -> [Post store] + [Fan-out svc]\n    normal followers -> push post_id -> [Feed cache: Redis per user]\n    high-follower      -> mark for pull\n Load feed -> feed cache (push) + pull celebrity posts -> hydrate -> [Ranker] -> paginate\n media -> [Object storage + CDN]",
    "wrap_up": "Hybrid fan-out (push for normal users, pull for celebrities) balances write amplification vs read latency; feeds are cached per user as post-id lists, hydrated and ranked at read time. The bottleneck — celebrity fan-out — is solved by the pull path; scale via caching, capped feed size, and a CDN for media.",
}

_DETAIL_CRAWLER = {
    "scope": "Design a scalable web crawler to fetch billions of pages for indexing. Clarify: scope (whole web / subset)? freshness? content types? politeness? Assumptions: billions of pages, HTML focus, polite (robots.txt + per-host rate), fresh-ish, distributed.",
    "functional": "1) Start from seed URLs. 2) Fetch pages. 3) Extract links and enqueue new URLs. 4) Store page content. 5) Avoid re-crawls / loops. 6) Respect robots.txt + politeness.",
    "nonfunctional": "Scalability (billions of URLs), politeness (don't hammer a host), robustness (traps, bad HTML), freshness, extensibility (new content types), efficiency.",
    "estimation": "1B pages/month ≈ 400 pages/s. ~500 KB/page -> ~500 TB/month raw -> object storage. A seen-URL set for billions of URLs -> Bloom filter (memory-efficient) + a backing store.",
    "api": "Internal pipeline (not user-facing). Components: URL Frontier, Fetcher, Parser, Content store, URL dedup, DNS resolver.",
    "data_model": "URL frontier: prioritized front queues + per-host politeness back queues. Seen-URLs: Bloom filter + store. Pages: object storage keyed by URL hash. Metadata (last-crawled, content checksum) in a DB.",
    "hld": "Seeds -> URL Frontier (priority + politeness) -> Fetcher (DNS resolve, robots.txt check, per-host rate limit) -> Parser (extract text + links) -> content dedup (checksum/simhash) -> URL extractor -> URL dedup (Bloom filter) -> back into the frontier. Stateless distributed workers; pages in object storage.",
    "hld_diagram": "Seeds -> [URL Frontier: priority + per-host queues]\n   -> Fetcher(robots, rate-limit, DNS cache) -> Parser\n   -> content dedup(simhash) -> extract URLs -> URL dedup(Bloom) -> Frontier\n   pages -> [Object storage]",
    "algorithms": "URL frontier: two-level queues — front (priority) + back (per-host politeness). URL dedup: Bloom filter (fast, tiny memory, small false-positive rate). Near-duplicate content: simhash/minhash. BFS traversal by default. DNS caching to avoid a resolver bottleneck. Politeness: per-host delay + robots.txt cache.",
    "deep_dive": "Politeness vs throughput: per-host back-queues keep one polite connection per host while thousands of hosts crawl in parallel. Traps/infinite spaces: depth limits, URL-pattern filters, content dedup. Freshness: re-crawl priority by estimated change frequency. Scale: shard the frontier by host hash; stateless fetchers. Robustness: timeouts + retry/backoff + tolerant HTML parsing. Storage dedup via content checksum.",
    "follow_ups": "Keep it polite yet fast? (per-host queues + global parallelism). Detect spider traps? (depth + dedup + pattern rules). Prioritize important pages? (PageRank-ish priority). Distributed frontier consistency? (shard by host). JS-rendered pages? (headless-browser fetchers). Freshness? (adaptive re-crawl).",
    "final_diagram": " Seeds -> [URL Frontier (sharded by host): front=priority, back=politeness]\n            |\n   {Fetcher workers} -> DNS cache -> robots.txt cache -> download\n            |\n         Parser -> extract links -> [Bloom filter seen-set] -> Frontier\n            |\n     content dedup (simhash) -> [Object storage] + metadata DB",
    "wrap_up": "A crawler is a smart URL frontier (priority + politeness) feeding stateless fetchers and parsers, with Bloom-filter URL dedup and simhash content dedup, storing pages in object storage. The hard parts — politeness, traps, freshness, dedup at scale — are handled by per-host queues, depth/pattern limits, adaptive re-crawl, and probabilistic dedup structures.",
}

_DETAIL_MQ = {
    "scope": "Design a distributed message queue / streaming log (Kafka-like). Clarify: pub-sub or point-to-point? ordering? durability? replay? throughput? Assumptions: high-throughput durable pub-sub log, ordered within a partition, replayable, at-least-once by default.",
    "functional": "Producers publish to topics; consumers subscribe and read; durable retention; ordering within a partition; consumer groups for parallel consumption; replay from an offset.",
    "nonfunctional": "High throughput (millions msgs/s), durability (no loss), horizontal scalability, low latency, fault tolerance, per-partition ordering.",
    "estimation": "1M msgs/s × 1KB = 1 GB/s ingest. 7-day retention -> ~600 TB. Partitions provide parallelism; brokers hold partition replicas; sequential disk writes drive throughput.",
    "api": "produce(topic, key, value); consume(topic, group) -> messages + offsets; commitOffset(group, partition, offset). Admin: createTopic(name, partitions, replicationFactor).",
    "data_model": "Topic -> N partitions; each partition = an append-only ordered log segmented into files; each message has an offset. Consumer group tracks a committed offset per partition. Partitions replicated across brokers (1 leader + followers, ISR).",
    "hld": "Producers append to a partition (chosen by key hash) on its leader broker; the leader replicates to followers; consumers in a group each own a subset of partitions and pull sequentially, tracking offsets. A coordinator (ZooKeeper/KRaft) manages metadata, leader election, and consumer-group rebalancing. Sequential disk I/O + zero-copy = throughput.",
    "hld_diagram": "Producer -(hash key)-> partition leader broker -> replicate to followers\n Topic: [P0][P1][P2] (append-only logs)\n Consumer group: c1->P0, c2->P1, c3->P2 (own offsets, pull)",
    "algorithms": "Partition by key hash for ordering + parallelism. Append-only log with sequential writes + segment files. Leader-follower replication with ISR (in-sync replicas) for durability. Consumer-group rebalancing to spread partitions. Offset tracking for at-least-once (exactly-once via idempotent producers + transactions). Zero-copy transfer.",
    "deep_dive": "Ordering is per-partition only — choose the partition key well. Delivery: at-least-once (commit after processing) by default; exactly-once needs idempotent producers + transactional commits. Durability: replication factor + ISR + acks=all. Rebalancing briefly pauses consumption — sticky assignment reduces churn. Retention: time/size-based; log compaction for keyed latest-value topics. Consumers pull (natural backpressure). Skewed keys create hot partitions.",
    "follow_ups": "Exactly-once? (idempotent producer + transactions). Global ordering? (single partition — caps throughput). Slow consumer? (pull model + lag monitoring). Broker failure? (leader re-election from ISR). Replay? (reset offset). Scale a topic? (add partitions — but changes key->partition mapping).",
    "final_diagram": " Producers -> [partitioner by key]\n Brokers: topic partitions (leader + follower replicas, ISR)\n   P0: leader B1 / followers B2,B3    P1: leader B2 ...\n Consumer groups pull; offsets committed; coordinator (KRaft) = metadata + rebalancing\n retention: segmented append-only logs on disk (sequential I/O + zero-copy)",
    "wrap_up": "A partitioned, replicated, append-only commit log: producers append by key hash, consumers pull and track offsets, leader-follower + ISR gives durability, consumer groups give parallelism, retention enables replay. Throughput comes from sequential disk I/O and zero-copy; the core trade-off is ordering (per-partition) vs parallelism.",
}

_DETAIL_AUTOCOMPLETE = {
    "scope": "Design search autocomplete/typeahead returning top suggestions as the user types. Clarify: how many suggestions? personalized? typo tolerance? QPS? latency budget? Assumptions: top 5-10, <100ms, huge volume, based on popular queries.",
    "functional": "As the user types a prefix, return the top-k most relevant/popular completions; reflect trending queries over time.",
    "nonfunctional": "Very low latency (<100ms end-to-end), high availability, scalable to high QPS, fresh-ish (trending), relevant ranking.",
    "estimation": "10M searches/day × ~5 keystrokes fetching suggestions = 50M suggestion requests/day ≈ 600/s (debounced on the client). A Trie of top queries fits in memory; precompute top-k per node.",
    "api": "GET /suggest?q=prefix -> [suggestions]. Data pipeline: aggregate query logs -> build/refresh the suggestion structure.",
    "data_model": "A Trie where each node = a prefix and stores/points to its top-k completions (with frequencies). Alternative: an edge-n-gram inverted index. Frequencies come from aggregated query logs.",
    "hld": "Offline: a pipeline aggregates raw query logs into (query, frequency), builds a Trie annotated with top-k completions per prefix node, and ships it to the serving layer (in-memory/Redis). Online: keystroke -> debounced request -> prefix lookup -> return precomputed top-k. Hot prefixes cached at the edge.",
    "hld_diagram": "Query logs -> [Aggregator] -> build Trie(top-k per node) -> serving (in-mem/Redis)\n Keystroke -> debounce -> GET /suggest?q=prefix -> top-k lookup -> suggestions",
    "algorithms": "Trie with precomputed top-k per node -> lookup is O(prefix length), not a scan. Precompute top-k bottom-up. Recency-decayed frequency for trending. Typo tolerance via edit-distance/fuzzy or an n-gram index. Count-Min sketch for approximate frequencies at scale.",
    "deep_dive": "Latency: precompute top-k so serving is a pointer walk; keep it in memory; debounce keystrokes; cache hot prefixes. Freshness vs cost: rebuild the Trie periodically (e.g., hourly) from logs, plus a streaming layer for trending. Scale: shard the Trie by leading characters; replicate for read QPS. Personalization: blend a global Trie with a small per-user history. Memory: compressed/ternary Trie, top-k only. Filter inappropriate suggestions with a blocklist.",
    "follow_ups": "Surface trending queries fast? (streaming layer + frequent rebuild). Typo tolerance? (fuzzy/n-gram). Personalized suggestions? (blend user history). Scale the Trie? (shard by prefix, replicate). Reduce memory? (compressed Trie). Filter offensive terms? (blocklist).",
    "final_diagram": " Query logs -> [Stream + batch aggregation] -> (query, weighted freq)\n            -> build Trie: each node -> precomputed top-k\n            -> ship to [Serving: in-memory Tries, sharded by prefix, replicated] + edge cache\n Client keystroke -> debounce -> /suggest?q=prefix -> O(len) top-k lookup -> render",
    "wrap_up": "A Trie annotated with precomputed top-k completions makes each suggestion an O(prefix) lookup; frequencies come from aggregated batch + streaming query logs, rebuilt periodically for freshness. Latency is won by precomputation + in-memory serving + client debounce + edge caching; extensions add typo tolerance and personalization.",
}


ENTRIES = [
    # ─────────── Core Building Blocks ───────────
    E("core", "Load Balancing",
      "A single server can't handle all traffic and is a single point of failure.",
      "A load balancer spreads requests across a server pool (Round Robin, Least Connections, IP Hash, Weighted). L4 balances on IP/port, L7 on HTTP content. Health checks evict dead nodes.",
      "AWS ALB fronts a fleet of catalog instances; sticky sessions pin a user to one node when needed.",
      "Horizontal scaling, high availability, zero-downtime deploys (drain + rotate).",
      "Client -> [Load Balancer] -> {App1, App2, App3}  (health-checked pool)",
      "Itself a potential SPOF (needs redundancy); adds a hop; L7 inspection costs CPU.",
      "NGINX, HAProxy, AWS ELB/ALB/NLB, Envoy, GCP Load Balancing; DNS round-robin (crude)."),
    E("core", "Caching",
      "Recomputing/re-fetching the same data is slow and overloads the database.",
      "Keep hot data in RAM. Patterns: cache-aside, read/write-through, write-back. Eviction: LRU/LFU/TTL. The hard part is invalidation (keeping cache consistent with source).",
      "Product pages cached in Redis with a 5-min TTL; on price change the key is invalidated.",
      "Catalog, sessions, computed feeds, rate-limit counters, hot inventory reads.",
      "Client -> App -> [Redis] --hit--> return; miss -> DB -> populate -> return",
      "Stale data; invalidation is hard; thundering herd on expiry; extra cost.",
      "Redis, Memcached, in-process (Caffeine/Guava), CDN edge, Hazelcast."),
    E("core", "CDN (Content Delivery Network)",
      "Serving static/media from one origin is slow for distant users and hammers the origin.",
      "Geo-distributed edge servers cache content near users; requests hit the nearest PoP. Pull (lazy) or push (pre-load).",
      "Images, CSS/JS, video served from CloudFront edges; origin (S3) hit only on miss.",
      "Images, static assets, video, cacheable API responses.",
      "User(IN)->Edge(Mumbai)-miss->Origin(S3); User(US)->Edge(Virginia)",
      "Cost; invalidation lag for updated assets; not for personalized/dynamic content.",
      "CloudFront, Akamai, Cloudflare, Fastly, GCP CDN."),
    E("core", "Message Queues / Async Processing",
      "Slow or spiky work shouldn't block the user request; services shouldn't be tightly coupled.",
      "Producers push messages; consumers process asynchronously at their own pace. Decouples, buffers spikes, enables retries. Delivery is usually at-least-once (needs idempotency).",
      "On checkout, publish OrderPlaced; email, inventory and analytics consume independently.",
      "Emails/notifications, order pipelines, media processing, decoupling, load leveling.",
      "Checkout -> [Queue] -> {Email, Inventory, Analytics} workers",
      "Eventual consistency; ordering & duplicate handling; another system to run; harder debugging.",
      "Kafka, RabbitMQ, AWS SQS/SNS, GCP Pub/Sub, ActiveMQ, Redis Streams."),
    E("core", "Event-Driven Architecture / Pub-Sub",
      "Many services must react to the same event without the producer knowing about them.",
      "Producers emit events to topics; any number of subscribers consume independently (fan-out). A durable log (Kafka) adds replay and event sourcing.",
      "OrderPlaced fans out to inventory, loyalty, recommendation retraining, seller dashboard.",
      "Microservice choreography, real-time analytics, CDC, audit logs.",
      "Producer -> [Topic] -> {Sub A, Sub B, Sub C}  (independent consumers)",
      "Hard to trace end-to-end; eventual consistency; schema evolution; 'event soup'.",
      "Kafka, AWS SNS+SQS, GCP Pub/Sub, NATS, EventBridge."),
    E("core", "Rate Limiting",
      "Protect services from abuse/bots/spikes and enforce quotas and fairness.",
      "Algorithms: Token Bucket (bursty), Leaky Bucket (smooth), Fixed/Sliding Window counters. Usually a Redis counter keyed by user/IP/API-key.",
      "Add-to-cart capped at N req/s per user in Redis to blunt bots during a flash sale.",
      "API gateways, login/OTP, scraping/bot defense, flash-sale fairness.",
      "Request -> [Redis token bucket per key] -> allow / 429",
      "Distributed counting is tricky (skew, races); false positives block users behind shared NAT.",
      "Redis limiters, Kong/AWS API Gateway, Envoy, Cloudflare."),
    E("core", "API Gateway",
      "Clients shouldn't call dozens of services directly; cross-cutting concerns repeat everywhere.",
      "A single entry point routing to backends, handling auth, rate limiting, TLS termination, aggregation, versioning.",
      "Mobile app -> Gateway (validates JWT, rate-limits) -> catalog/cart/order services.",
      "Microservice front door, BFF, centralized auth.",
      "Clients -> [API Gateway] -> {Catalog, Cart, Order, ...}",
      "Potential bottleneck/SPOF; extra latency; can become a 'god' component.",
      "Kong, AWS API Gateway, Apigee, NGINX, Envoy, Spring Cloud Gateway."),
    E("core", "Microservices vs Monolith",
      "Scaling teams and components independently as a system grows.",
      "Monolith: one deployable, simple, scales as a unit. Microservices: many small independently deployable services owning their data, communicating via API/events.",
      "Split a monolith into catalog, cart, order, payment, inventory, search services.",
      "Large orgs, differing scale profiles, independent release cadences.",
      "Monolith: [ALL]  |  Microservices: {svc}-{svc}-{svc} + events",
      "Distributed complexity (network, partial failure, data consistency), ops overhead. Start monolith, split when justified.",
      "Modular monolith, SOA, serverless functions."),
    E("core", "CAP Theorem & Consistency Models",
      "Reasoning about trade-offs in distributed stores during network partitions.",
      "Under a partition, choose Consistency (block/stale-reject) or Availability (serve possibly-stale). Models: strong, eventual, causal, read-your-writes. PACELC adds latency-vs-consistency when no partition.",
      "Payments choose CP (correctness); catalog/reviews choose AP (always available, eventually consistent).",
      "Picking/tuning databases per workload.",
      "Partition present -> pick C or A; else -> pick Latency or Consistency (PACELC)",
      "No free lunch; 'eventual' surprises users (stale carts).",
      "CP: HBase, Spanner, Zookeeper. AP: Cassandra, Dynamo. Tunable: Cassandra quorums."),
    E("core", "Idempotency",
      "Retries / at-least-once delivery can process the same request twice (double charge/order).",
      "Client sends an idempotency key; server records it and returns the same result on repeats. Or design naturally idempotent ops (upserts, set-state).",
      "'Place order' carries an idempotency key; a retried checkout returns the existing order.",
      "Payments, order creation, any retried/queued write.",
      "Request(key) -> if key seen: return stored result; else execute + store",
      "Must store keys (TTL); concurrent duplicates need a unique constraint/lock.",
      "Stripe-style keys, DB unique constraints, dedup tables, exactly-once frameworks."),
    E("core", "CQRS (Command Query Responsibility Segregation)",
      "Read and write workloads have very different shapes and scale needs.",
      "Separate the write model (normalized, transactional) from the read model (denormalized, query-optimized), synced via events.",
      "Orders written to Postgres; a denormalized 'order history' projected into Elasticsearch for fast reads.",
      "High-read systems, complex reporting, event-sourced systems.",
      "Command -> Write DB -> events -> Read model(s) <- Query",
      "Complexity; eventual consistency between models; more moving parts.",
      "Plain CRUD, materialized views, read replicas."),
    E("core", "Saga Pattern (Distributed Transactions)",
      "No ACID transaction spans microservices, but a business flow (checkout) touches many.",
      "A sequence of local transactions, each emitting an event triggering the next; on failure run compensating transactions. Choreography (events) or orchestration (coordinator).",
      "Checkout saga: reserve inventory -> charge -> create order; on failure, release inventory.",
      "Order fulfillment, bookings, any multi-service transaction.",
      "Order -> reserve -> charge -> confirm ; compensate backwards on failure",
      "Complex to design/debug; eventual consistency; compensations for money are delicate.",
      "2PC (blocking, avoided), Temporal/Camunda orchestration, outbox pattern."),
    E("core", "Consistent Hashing",
      "Adding/removing nodes shouldn't remap almost all keys.",
      "Map nodes and keys onto a hash ring; a key belongs to the next node clockwise. Node changes only move adjacent keys. Virtual nodes even out load.",
      "Distributing cache keys across a Redis cluster; Dynamo/Cassandra partitioning.",
      "Distributed caches, sharded DBs, load balancers.",
      "[hash ring] key -> next node clockwise; +node moves only nearby keys",
      "Uneven load without vnodes; still some remap; more complex than modulo.",
      "Modulo hashing (simple, bad on resize), rendezvous hashing, range partitioning."),
    E("core", "Observability (Logs, Metrics, Tracing)",
      "You can't fix or scale what you can't see, especially across microservices.",
      "Logs (events), metrics (latency/QPS/error time-series), distributed tracing (a request's path via a trace ID). Alert on SLOs.",
      "A slow checkout is traced across cart->payment->inventory to find the bottleneck.",
      "Debugging, capacity planning, SLA/SLO monitoring, incident response.",
      "Services -> {logs, metrics, traces} -> collector -> dashboards/alerts",
      "Telemetry volume/cost; cardinality explosions; noise vs signal.",
      "Prometheus+Grafana, ELK/OpenSearch, Datadog, Jaeger/Zipkin, OpenTelemetry."),

    # ─────────── Databases & Data Stores ───────────
    E("data_stores", "SQL vs NoSQL",
      "Choosing storage for a given data shape, scale, and consistency need.",
      "SQL: structured schema, relations/joins, ACID, strong consistency, vertical scale. NoSQL: flexible schema, horizontal scale, often eventual; families key-value, document, wide-column, graph.",
      "Orders/payments in Postgres (ACID); catalog/cart in DynamoDB (scale); recs graph in Neo4j.",
      "SQL for transactions & complex queries; NoSQL for scale, high write throughput, flexible data.",
      "SQL: rows+joins+ACID  |  NoSQL: KV / doc / wide-column / graph",
      "SQL harder to scale horizontally; NoSQL sacrifices joins/consistency, pushes logic to the app.",
      "Postgres, MySQL, Aurora / DynamoDB, Cassandra, MongoDB, Neo4j; Spanner (NewSQL bridge)."),
    E("data_stores", "Database Indexing",
      "Full-table scans are O(n) and slow for lookups/filters.",
      "An index (B-tree, or hash for equality) maps column values to row locations, making lookups O(log n). Composite indexes serve multi-column queries; covering indexes serve a query fully from the index.",
      "Index on orders(user_id, created_at) makes 'my recent orders' instant.",
      "Frequent WHERE/JOIN/ORDER BY columns, foreign keys, uniqueness.",
      "value -> [B-tree] -> row location (O(log n))",
      "Slows writes; uses storage; too many indexes hurt; unused indexes are pure cost.",
      "B-tree, Hash, GiST/GIN (Postgres), LSM-tree (write-heavy), inverted index (search)."),
    E("data_stores", "Sharding / Partitioning",
      "A single DB can't hold all the data or serve all the traffic.",
      "Split data across nodes by a shard key: hash-based (even, range-queries hard), range-based (range-friendly, hotspot risk), directory-based. Consistent hashing minimizes reshuffling.",
      "Shard orders by hash(user_id) so a user's orders live on one shard.",
      "Very large tables (orders, events, catalog at marketplace scale).",
      "Router/hash -> {Shard1, Shard2, Shard3}",
      "Cross-shard joins/txns hard; rebalancing painful; hotspots on skewed keys; ops complexity.",
      "Vitess (MySQL), Citus (Postgres), native (Mongo/Cassandra), app-level sharding."),
    E("data_stores", "Replication",
      "Availability, read scaling, and durability if a node dies.",
      "Copy data to multiple nodes. Leader-follower: writes to leader, reads from followers (async/sync). Leaderless/multi-leader (Dynamo quorum W+R>N) for write availability.",
      "Postgres primary handles writes; 3 read replicas serve catalog reads; failover promotes a replica.",
      "Read-heavy workloads, HA, geo-distribution, backups.",
      "Writes -> Leader -> async -> {Follower1, Follower2} <- reads",
      "Replication lag -> stale reads; sync replication adds latency; multi-leader conflicts.",
      "Postgres/MySQL replication, Mongo replica sets, Cassandra quorums, DynamoDB global tables."),
    E("data_stores", "TiDB (Distributed NewSQL / HTAP)",
      "Teams want MySQL-compatible SQL and ACID transactions but also horizontal scale and real-time analytics on the same data, without bolting on a separate warehouse.",
      "TiDB is an open-source distributed NewSQL database with a disaggregated architecture: stateless SQL layer (TiDB) + a distributed transactional KV store (TiKV, row-based, Raft-replicated, range-sharded into 'Regions') + a columnar engine (TiFlash) kept in sync via Raft learners. PD (Placement Driver) manages metadata, timestamps (TSO) and auto-rebalancing. It speaks the MySQL protocol, gives strong consistency (Percolator-style distributed transactions), and does HTAP: OLTP on TiKV, OLAP on TiFlash — the optimizer routes each query to the right engine.",
      "A payments/orders system that outgrew a single MySQL: shard-free horizontal scale on TiKV for transactions, while dashboards run analytical queries on TiFlash over the same live data — no ETL to a warehouse.",
      "Horizontally-scaling OLTP that outgrew MySQL, HTAP (fresh analytics on transactional data), multi-region strong consistency, MySQL migration without app rewrites.",
      "App -(MySQL proto)-> [TiDB SQL nodes] -> TiKV (row, OLTP) + TiFlash (columnar, OLAP)\n                                     ^ PD: metadata, TSO, rebalancing",
      "Operationally heavier than a single MySQL (many components); distributed-txn latency vs local; TiFlash adds storage; not ideal for tiny workloads; some MySQL feature gaps.",
      "CockroachDB, Google Spanner, YugabyteDB, Vitess (sharded MySQL), Aurora; Snowflake/ClickHouse for pure OLAP."),
    E("data_stores", "Aerospike (Real-time NoSQL KV)",
      "Applications need single-digit-millisecond reads/writes over terabytes-to-petabytes at very high throughput (100k–millions of ops/sec) with strong reliability and low cost.",
      "Aerospike is a distributed key-value/row store built for speed: a hybrid-memory architecture keeps the primary index in RAM but data on SSD (accessed directly, bypassing the filesystem), so it gets near-in-memory latency at SSD cost/capacity. It shards data with a smart-client + consistent-hash partition map (no proxy hop), replicates synchronously, self-heals, and offers strong-consistency or AP modes. Cross-datacenter replication (XDR) enables global low-latency.",
      "A real-time ad-tech bidder or fraud-scoring service reading a user profile in <1ms during a request; a recommendation feature store serving models at scale.",
      "Real-time bidding/ad-tech, fraud detection, recommendation/feature stores, session stores, any large low-latency KV workload.",
      "Smart client -(partition map)-> {Aerospike node} RAM index + SSD data; sync replica; XDR -> other DC",
      "Data-model is KV/row (not relational, limited joins/queries); operationally specialized; licensing for enterprise features; overkill for small/low-QPS apps.",
      "Redis (in-memory, smaller data), DynamoDB, Cassandra/ScyllaDB, Memcached, Couchbase."),
    E("data_stores", "Redis (In-memory Data Structure Store)",
      "You need microsecond-to-millisecond access to hot data and rich primitives beyond a plain cache.",
      "In-memory store with data structures (strings, hashes, lists, sets, sorted sets, streams, HLL, geo). Single-threaded command execution (atomic), optional persistence (RDB/AOF), replication, and Cluster mode for sharding. Also does pub/sub, Lua scripts, and atomic counters.",
      "Cart/session store, rate-limit token buckets, leaderboards (sorted sets), Redis Streams as a lightweight queue.",
      "Caching, sessions, rate limiting, leaderboards, queues, real-time counters, geo lookups.",
      "App -> [Redis] (RAM) --async--> AOF/RDB persistence; Cluster shards by hash slot",
      "RAM is expensive/limited; persistence is best-effort (can lose recent writes); large datasets need Cluster; not a system of record.",
      "Memcached (simpler cache), Aerospike (bigger-than-RAM), Hazelcast, DynamoDB DAX."),
    E("data_stores", "Cassandra (Wide-Column, AP)",
      "Massive write throughput and always-on availability across regions, with no single master.",
      "Leaderless, masterless wide-column store. Data partitioned by hash of the partition key across a ring; tunable consistency via quorums (W+R>N). LSM-tree storage (fast writes). You model tables per query (denormalized).",
      "Time-series/event data, order history at hyperscale, messaging inboxes.",
      "Write-heavy workloads, time-series, multi-region availability, huge datasets.",
      "Client -> any node (coordinator) -> replicas on ring; tunable quorum reads/writes",
      "No joins; query-first data modeling; eventual consistency by default; tombstone/compaction pitfalls.",
      "ScyllaDB (C++ rewrite), DynamoDB, HBase, Bigtable."),
    E("data_stores", "Elasticsearch (Search / Inverted Index)",
      "SQL LIKE is slow and can't do relevance, typos, or facets.",
      "An inverted index maps terms -> documents; adds tokenization, stemming, BM25 relevance, fuzzy matching, and aggregations/facets. Distributed via shards + replicas.",
      "Product search with typo tolerance, brand/price filters, and relevance ranking; also log analytics.",
      "Product search, autocomplete, log/observability analytics, faceted filtering.",
      "Docs -> [inverted index shards] ; query -> BM25 rank + facets",
      "Eventually consistent with source DB; ops-heavy; not a system of record.",
      "OpenSearch, Apache Solr, Algolia, Typesense, Postgres full-text (small scale)."),
    E("data_stores", "Object / Blob Storage",
      "Databases are wrong for large binary files (images, video, invoices).",
      "Store files as objects with metadata in a flat namespace over HTTP; near-unlimited, cheap, durable. Fronted by a CDN; uploads via pre-signed URLs.",
      "Product images and invoices in S3; the DB stores only the URL/key.",
      "Media, backups, static sites, data-lake storage.",
      "App -> pre-signed URL -> [S3 object] -> CDN edge -> user",
      "Higher latency than block storage; historically eventual consistency; not for frequent small updates.",
      "AWS S3, GCS, Azure Blob, MinIO, Ceph."),
    E("data_stores", "Distributed SQL (Spanner / CockroachDB)",
      "You want relational SQL + ACID transactions that also scale horizontally and survive region failures.",
      "Globally-distributed SQL: data auto-sharded into ranges, each Raft/Paxos-replicated; distributed transactions with synchronized clocks (Spanner's TrueTime) give external consistency. Looks like one SQL DB, scales like NoSQL.",
      "A global orders ledger needing strong consistency and multi-region survivability.",
      "Global OLTP, strong consistency at scale, multi-region resilience, MySQL/Postgres-compatible scale-out.",
      "SQL -> distributed txn coordinator -> Raft-replicated ranges (multi-region)",
      "Higher write latency (consensus + clock bounds); cost; operational complexity; some SQL limits.",
      "Google Spanner, CockroachDB, YugabyteDB, TiDB, Aurora (limited)."),

    # ─────────── Data Platforms at Scale ───────────
    E("data_platforms", "Data Warehouse (OLAP)",
      "Running heavy analytical queries on the production (OLTP) database slows the app and can't handle the scan volume.",
      "A columnar, analytics-optimized store for structured data, loaded via ETL/ELT from source systems. Columnar layout + massively parallel processing (MPP) make aggregations over billions of rows fast. Separates analytics from transactions.",
      "Sales/orders/clickstream loaded nightly (and via streaming) into Snowflake; BI dashboards and analysts query it.",
      "BI dashboards, reporting, ad-hoc analytics, ML feature generation.",
      "OLTP sources -> ETL/ELT -> [Warehouse (columnar MPP)] -> BI / SQL / ML",
      "Data latency (batch); cost at scale; schema rigidity; not for low-latency point lookups.",
      "Snowflake, BigQuery, Redshift, Databricks SQL, Synapse; ClickHouse (real-time OLAP)."),
    E("data_platforms", "Data Lake",
      "Warehouses are expensive and rigid for raw, semi/unstructured, or exploratory data at petabyte scale.",
      "Store raw data of any shape (JSON, logs, images, Parquet) cheaply in object storage, schema-on-read. Decouples storage from compute; many engines can query the same files.",
      "Raw clickstream, logs, images, and CDC dumps land in S3; Spark/Athena query on demand.",
      "Data science, ML training data, cheap archival, staging before the warehouse.",
      "Sources -> [Object storage (raw files)] <- query engines (Spark/Athena/Presto)",
      "Can become a 'data swamp' without governance; slow ad-hoc queries; no ACID/consistency by itself.",
      "S3/GCS/ADLS + Spark/Presto/Athena; vs warehouse (structured) and lakehouse (hybrid)."),
    E("data_platforms", "Data Lakehouse (Medallion Architecture)",
      "Teams don't want to maintain both a data lake (cheap, flexible) and a warehouse (fast, governed) with brittle copies between them.",
      "A lakehouse adds a transactional table format (ACID, schema enforcement, time travel) on top of cheap object storage, so one platform serves BI and ML. The medallion pattern layers data: Bronze (raw) -> Silver (cleaned/conformed) -> Gold (business-level aggregates).",
      "Databricks/Delta Lake: raw events -> Bronze; deduped/typed -> Silver; daily revenue marts -> Gold, all in S3, queried by SQL and Spark ML.",
      "Unified BI + ML on one copy, streaming + batch together, governed lake, cost-efficient scale.",
      "Sources -> Bronze(raw) -> Silver(clean) -> Gold(marts) on object storage (Delta/Iceberg)\n            + ACID table format -> BI & ML engines",
      "Younger tooling; table-format lock-in choices; still needs governance discipline; small-query latency vs a tuned warehouse.",
      "Databricks/Delta Lake, Apache Iceberg, Apache Hudi; vs Snowflake (warehouse) + separate lake."),
    E("data_platforms", "Open Table & File Formats (Iceberg/Delta/Hudi, Parquet)",
      "Raw files in a lake lack ACID, schema evolution, and efficient updates; row formats are slow for analytics.",
      "Columnar file formats (Parquet/ORC) compress well and enable column pruning for fast scans. Table formats (Apache Iceberg, Delta Lake, Hudi) add a metadata layer over those files giving ACID transactions, schema/partition evolution, time travel, and upserts on the lake.",
      "Iceberg tables over Parquet in S3 let multiple engines (Spark, Trino, Flink) read/write consistently with snapshots and rollbacks.",
      "Lakehouse storage, CDC upserts into a lake, multi-engine interoperability, GDPR deletes.",
      "Query engines -> [Table format metadata (Iceberg)] -> Parquet files in object storage",
      "Metadata/small-file management overhead; format wars/interop nuances; compaction needed.",
      "Iceberg, Delta Lake, Hudi (table formats); Parquet, ORC, Avro (file formats)."),
    E("data_platforms", "Data Mesh",
      "A single central data team becomes a bottleneck; centralized pipelines don't scale organizationally across many domains.",
      "A decentralized, organizational approach: domain teams own their data as 'data products' (discoverable, addressable, trustworthy, self-describing), on a self-serve platform, under federated computational governance. Shifts from centralized ownership to domain ownership.",
      "The 'orders' domain publishes a curated orders data product; the 'marketing' domain consumes it without a central team brokering every request.",
      "Large orgs with many domains, scaling data ownership, reducing central-team bottlenecks.",
      "Domain A -> data product | Domain B -> data product | ... on shared self-serve platform + federated governance",
      "Mostly an org/operating-model shift (hard); risk of duplication/inconsistency; needs strong platform + governance; can be overkill for small orgs.",
      "Centralized warehouse/lake team, Data Fabric (more tech-centric), hub-and-spoke."),
    E("data_platforms", "Change Data Capture (CDC)",
      "Downstream systems (search, cache, warehouse, other services) need to stay in sync with a source DB without dual-writes or slow polling.",
      "CDC streams row-level changes by tailing the database's write-ahead log/binlog and publishing insert/update/delete events (often to Kafka). Consumers apply them to keep replicas/indexes/warehouses current in near-real-time.",
      "Debezium reads Postgres WAL -> Kafka -> indexer updates Elasticsearch and loads the warehouse.",
      "Cache/search sync, warehouse ingestion, event-driven microservices, replication, outbox pattern.",
      "Source DB (WAL/binlog) -> [CDC (Debezium)] -> Kafka -> {Search, Cache, Warehouse}",
      "Ordering & schema-evolution handling; initial snapshot cost; exactly-once is hard; log retention.",
      "Debezium, Maxwell, AWS DMS, Fivetran, native logical replication."),
    E("data_platforms", "Batch vs Stream Processing (Spark/Flink)",
      "Some analytics can run on schedules over big datasets; others must react within seconds to live events.",
      "Batch processes large bounded datasets periodically (high throughput, high latency) — Spark/MapReduce. Stream processing handles unbounded events continuously with low latency and windowing/state — Flink/Spark Structured Streaming/Kafka Streams. Lambda architecture runs both; Kappa uses one streaming path.",
      "Nightly Spark job builds revenue marts (batch); Flink computes real-time 'trending products' from clickstream (stream).",
      "ETL/aggregations (batch); real-time metrics, fraud, alerting, personalization (stream).",
      "Bounded data -> [Spark batch] -> marts | Event stream -> [Flink] -> real-time views",
      "Batch: stale data. Stream: complex state/windowing, exactly-once & late-event handling, harder ops.",
      "Spark, Flink, Kafka Streams, Beam/Dataflow, Storm; Lambda vs Kappa architectures."),
    E("data_platforms", "Feature Store",
      "ML features computed ad-hoc drift between training and serving (train/serve skew) and get re-implemented per team.",
      "A central system to define, compute, store, and serve ML features consistently. Offline store (warehouse/lake) for training; online store (low-latency KV like Redis/Aerospike/DynamoDB) for real-time inference; the same definitions feed both to prevent skew.",
      "A 'user_30d_purchase_count' feature is computed once, served from the online store to the ranking model at request time and from the offline store for training.",
      "Recommendations, fraud, ranking, any real-time ML; feature reuse and governance.",
      "Sources -> feature pipelines -> {Offline store (train), Online store (serve)} -> models",
      "Infra complexity; consistency between online/offline; freshness vs cost; another platform to run.",
      "Feast, Tecton, Databricks/Vertex/SageMaker Feature Store; DIY Redis + warehouse."),

    # ─────────── Data Analytics ───────────
    E("data_analytics", "OLTP vs OLAP",
      "One database can't be great at both fast small transactions and heavy analytical scans.",
      "OLTP: many small, low-latency read/write transactions on current data, row-oriented, normalized (the app DB). OLAP: few large aggregation/scan queries over history, column-oriented, denormalized (the warehouse). Keep them separate; move data OLTP -> OLAP via ETL/CDC.",
      "Postgres serves the checkout (OLTP); Snowflake serves 'revenue by category last quarter' (OLAP).",
      "OLTP for the app; OLAP for BI, reporting, data science.",
      "App writes -> [OLTP row store] --ETL/CDC--> [OLAP column store] -> analysts",
      "Two systems to keep in sync; analytics data lags; cost of the pipeline.",
      "OLTP: Postgres/MySQL. OLAP: Snowflake/BigQuery/ClickHouse. HTAP (both): TiDB, SingleStore."),
    E("data_analytics", "Real-time / Streaming Analytics",
      "Batch reports are hours stale; some decisions (fraud, trending, ops alerts) need sub-second data.",
      "Ingest events into a stream (Kafka), process continuously with windowed aggregations and stateful operators (Flink), and serve results from a low-latency OLAP store or cache. Handle late/out-of-order events with watermarks.",
      "Live 'trending now' and real-time GMV dashboards during a sale, computed from the clickstream in seconds.",
      "Fraud, real-time dashboards, trending, anomaly detection, dynamic pricing signals.",
      "Events -> Kafka -> [Flink windows] -> real-time OLAP (Druid/ClickHouse) -> dashboard",
      "Complex state & exactly-once; late-data handling; higher ops cost than batch; harder to debug.",
      "Flink, Kafka Streams, Spark Streaming, Materialize, Apache Druid, Pinot, ClickHouse."),
    E("data_analytics", "BI, Dashboards & the Semantic Layer",
      "Analysts and execs need self-serve, consistent metrics without writing raw SQL or disagreeing on definitions.",
      "BI tools query the warehouse and visualize results. A semantic/metrics layer centralizes metric definitions (e.g., 'active user', 'GMV') so every dashboard computes them identically. Caching/extracts speed up dashboards.",
      "Looker/LookML (or dbt metrics) defines 'net revenue' once; Tableau/Looker dashboards reuse it.",
      "Executive dashboards, self-serve analytics, KPI reporting, consistent metrics.",
      "Warehouse -> [Semantic layer (metric defs)] -> BI tool -> dashboards",
      "Metric sprawl without governance; performance on huge data; extract staleness; tool lock-in.",
      "Looker, Tableau, Power BI, Superset, Metabase; dbt Semantic Layer, Cube, AtScale."),
    E("data_analytics", "ETL vs ELT",
      "Getting source data into an analytics store in the right shape, at scale, reliably.",
      "ETL: Extract, Transform (in a separate engine), then Load — transform before landing (good for constrained targets/compliance). ELT: Extract, Load raw, then Transform inside the powerful warehouse/lakehouse using SQL — the modern default given cheap elastic compute.",
      "Fivetran extracts app data and loads raw into Snowflake; dbt transforms it into clean marts (ELT).",
      "Warehouse/lake loading, data pipelines, analytics engineering.",
      "ETL: Extract -> Transform -> Load | ELT: Extract -> Load(raw) -> Transform (in warehouse)",
      "ETL: rigid, extra engine. ELT: raw sprawl, transform cost in the warehouse, needs governance.",
      "Ingest: Fivetran, Airbyte, DMS. Transform: dbt, Spark. Orchestrate: Airflow, Dagster."),
    E("data_analytics", "Clickstream / Event Analytics Pipeline",
      "Capture billions of user events (views, clicks, add-to-cart) for analytics, personalization, and BI.",
      "Client/edge emits events -> ingestion (Kafka) -> real-time stream processing for live metrics AND a sink to the lake/warehouse for batch. Lambda/Kappa architecture; downstream funnels, A/B, ML features.",
      "Events -> Kafka -> Flink (real-time metrics) + S3 -> warehouse (batch) -> BI + feature store.",
      "Funnels, A/B analysis, recommendation features, real-time dashboards, product analytics.",
      "Client -> collector -> Kafka -> {Flink -> real-time metrics ; Lake -> warehouse -> BI/ML}",
      "Data volume/cost; exactly-once vs at-least-once; schema evolution; late/out-of-order events.",
      "Kafka/Kinesis, Flink/Spark, Snowflake/BigQuery, Segment/RudderStack, Amplitude."),
    E("data_analytics", "A/B Testing & Experimentation Platform",
      "Teams need to measure whether a change actually improves a metric, at scale, without fooling themselves.",
      "Randomly assign users to variants (bucketing by hashed user id), log exposures + outcomes, and compute statistically-sound lifts (with guardrail metrics, sequential-testing corrections). A platform manages assignment, metrics, and analysis consistently.",
      "New checkout flow shipped to 5% behind an experiment; watch-time/conversion measured vs control before ramping.",
      "Feature launches, ranking/rec changes, pricing tests, UX changes.",
      "User -> hash bucket -> variant; exposures+events -> analysis -> lift & significance",
      "Peeking/false positives; interaction effects; network effects; underpowered tests; metric gaming.",
      "Optimizely, LaunchDarkly, Statsig, GrowthBook, in-house (Amazon Weblab / Google-style)."),

    # ─────────── Data Modeling ───────────
    E("data_modeling", "Common Data Model (Canonical Model)",
      "When many apps/services/partners each define 'customer' or 'order' differently, every integration needs custom mapping (N-to-N), and analytics can't join across systems.",
      "A Common (Canonical) Data Model is a shared, standardized schema for core business entities that all systems map to once. Integrations translate to/from the canonical model (N-to-1 instead of N-to-N). Industry CDMs (Microsoft Common Data Model / Dataverse, healthcare's OMOP, retail ARTS) provide ready entity definitions; you can also define your own enterprise canonical model.",
      "An enterprise defines a canonical 'Customer' entity; CRM, billing, and the webshop each map to it, so a unified 360 view and cross-system analytics 'just work'.",
      "Enterprise integration (canonical model in an ESB/iPaaS), master data management, data warehousing conformance, partner/EDI exchange, unifying microservice data for analytics.",
      "System A -> map -> [Canonical Model] <- map <- System B ; consumers read canonical (N-to-1)",
      "Upfront design + governance effort; a rigid canonical model can lag business change; over-generalization; someone must own it.",
      "Point-to-point mapping (simpler, doesn't scale), Microsoft CDM/Dataverse, industry models (OMOP, ARTS, FHIR), MDM tools."),
    E("data_modeling", "Dimensional Modeling (Star / Snowflake Schema)",
      "Normalized OLTP schemas are slow and unintuitive for analytics with many joins.",
      "Model analytics as facts (measurable events, e.g., a sale with quantity/amount) surrounded by dimensions (descriptive context: product, customer, date, store). Star schema = denormalized dimensions around a fact table; snowflake = normalized dimensions. Optimized for slicing/aggregating.",
      "A 'sales_fact' table joins to dim_product, dim_customer, dim_date, dim_store for BI queries like 'revenue by category by month'.",
      "Data warehouses, BI, reporting, cubes.",
      "dim_product \\ \n dim_date  -- [ fact_sales ] -- dim_customer \n dim_store /",
      "ETL to maintain; denormalization duplication; not for OLTP; grain mistakes cause wrong metrics.",
      "Data Vault (more flexible/auditable), One Big Table (wide denormalized), 3NF warehouse (Inmon)."),
    E("data_modeling", "Normalization vs Denormalization",
      "Balancing data integrity/write-efficiency against read/query performance.",
      "Normalization (1NF->3NF) removes redundancy so each fact lives once — great for OLTP integrity and cheap writes, but reads need joins. Denormalization duplicates data to avoid joins — faster reads, but writes must update copies and risk inconsistency. Choose by read/write ratio and consistency needs.",
      "OLTP orders schema is normalized; the analytics/read model and NoSQL catalog docs are denormalized for fast reads.",
      "OLTP (normalize), OLAP/read models/NoSQL (denormalize), caching layers.",
      "Normalized: many small tables + joins | Denormalized: fewer wide tables, duplicated data",
      "Normalized: join-heavy reads. Denormalized: update anomalies, storage, consistency burden.",
      "3NF (Inmon) vs star schema (Kimball) vs Data Vault; NoSQL denormalized docs."),
    E("data_modeling", "Slowly Changing Dimensions (SCD)",
      "Dimension attributes change over time (a customer moves cities); how do you keep history for correct point-in-time analytics?",
      "Techniques to handle dimension changes: Type 1 (overwrite, no history), Type 2 (add a new row with effective dates / current flag — full history), Type 3 (add a 'previous value' column — limited history). Type 2 is the workhorse for auditable analytics.",
      "A customer's address change adds a new SCD-Type-2 row so historical orders still map to the address at purchase time.",
      "Data warehousing, historical/point-in-time reporting, audit, regulatory analytics.",
      "SCD2: cust_key | attrs | valid_from | valid_to | is_current  (new row per change)",
      "Type 2 grows dimension size and complicates joins; Type 1/3 lose history; ETL complexity.",
      "CDC + merge logic, dbt snapshots, Data Vault satellites."),

    # ─────────── E-commerce Scenarios ───────────
    E("ecommerce", "Product Catalog Service",
      "Store and serve millions of products with fast, flexible, read-heavy access.",
      "Read-heavy (~100:1). Document store for flexible attributes; aggressive caching; images via CDN; denormalize for the product page; stream changes to search (CQRS-lite).",
      "Product docs in DynamoDB/Mongo, Redis for hot items, images in S3+CloudFront, CDC to search.",
      "Product detail page, listings, category browse.",
      "Client -> CDN -> API -> {Redis, Catalog DB} ; CDC -> Search index",
      "Cache invalidation on price/stock; eventual consistency to search; denormalization duplication.",
      "DynamoDB / MongoDB / Postgres+JSONB; Redis vs Memcached."),
    E("ecommerce", "Product Search & Autocomplete",
      "Fast, relevant, typo-tolerant search over the whole catalog with filters/facets.",
      "Index products in Elasticsearch (BM25); autocomplete via prefix/edge-n-grams or a Redis Trie; sync via CDC; rank by relevance + business signals (popularity, margin, availability).",
      "Search service consuming ProductUpdated into ES; autocomplete from a Redis Trie of top queries.",
      "Search bar, autocomplete, faceted filtering, 'did you mean'.",
      "Catalog -(events)-> Indexer -> ES ; search -> ES + Redis Trie",
      "Index lag; relevance tuning is ongoing; ES ops cost.",
      "Elasticsearch/OpenSearch, Algolia, Solr, Typesense."),
    E("ecommerce", "Shopping Cart",
      "Persist a user's items across sessions/devices with low latency and near-100% availability.",
      "Favor availability (AP). KV doc keyed by user/session in Redis/DynamoDB with TTL for guests; merge guest cart on login; validate price/stock at checkout, not on every add.",
      "Cart in DynamoDB (user_id -> items[]), Redis in front; guest carts by session cookie (30-day TTL).",
      "Add/remove/update items, cross-device cart, save-for-later.",
      "Client -> Cart API -> Redis -> DynamoDB ; checkout -> validate price/stock",
      "Stale prices/stock until checkout; guest->user merge conflicts; cross-region eventual consistency.",
      "Redis, DynamoDB, MongoDB; session vs persistent store."),
    E("ecommerce", "Checkout & Order Placement",
      "Turn a cart into a paid order reliably across inventory, payment, order — no double-charge or oversell.",
      "Orchestrate a Saga: reserve inventory -> charge -> create order -> confirm; compensate on failure. Idempotency key prevents duplicate orders; outbox pattern publishes events atomically with the DB write.",
      "Checkout orchestrator calls inventory, payment, order; on payment failure releases the reservation.",
      "The core buy flow.",
      "Cart -> Orchestrator: reserve -> charge(key) -> create(outbox) ; compensate on fail",
      "Distributed-transaction complexity; partial failures; eventual consistency; money compensations are delicate.",
      "Orchestration (Temporal/Step Functions) vs choreography; 2PC (avoided)."),
    E("ecommerce", "Payment Processing",
      "Charge customers correctly, securely, and exactly once via external gateways.",
      "Never store raw cards (PCI-DSS) — tokenize. Idempotency keys stop double charges. Payment state machine (initiated->authorized->captured->refunded). Handle async webhooks; reconcile daily.",
      "Stripe/Razorpay PaymentIntent with idempotency key; confirm via webhook; store only tokens.",
      "Card/UPI/wallet payments, refunds, subscriptions.",
      "Checkout -> Payment Svc -> Gateway ; webhook -> state machine + reconcile",
      "External dependency/latency; webhook reliability; fraud/chargebacks; strict compliance.",
      "Stripe, Razorpay, Braintree, Adyen, PayPal."),
    E("ecommerce", "Inventory Management (Avoid Oversell)",
      "Under high concurrency two buyers must not both buy the last unit.",
      "The last-item problem needs strong consistency on the counter: atomic conditional update (UPDATE ... SET qty=qty-1 WHERE qty>0), or reserve-on-add with TTL, or Redis atomic DECR + async DB reconciliation for flash-sale scale.",
      "Reserve stock at checkout via a conditional update; release on payment failure or reservation timeout.",
      "Stock display, reservations, flash sales, multi-warehouse.",
      "Checkout -> atomic: UPDATE stock SET qty=qty-1 WHERE sku=? AND qty>0",
      "Strong consistency caps throughput (hot SKUs); reservation cleanup; multi-warehouse allocation.",
      "SQL row locks, Redis atomic ops, Redlock, event-sourced stock."),
    E("ecommerce", "Order Management & Fulfillment",
      "Track an order through its lifecycle across many services.",
      "Explicit state machine (placed->paid->packed->shipped->delivered->returned) driven by events; orders in an ACID DB (source of truth); project a read model for history/tracking; emit events to warehouse/shipping/notify/analytics.",
      "Order service persists state; each fulfillment step emits an event updating status and notifying the customer.",
      "Order tracking, returns, support, seller dashboards.",
      "Order (Postgres) -> events -> {Warehouse, Shipping, Notify, Analytics} ; projection -> history",
      "Many states/edge cases (partial shipments, cancels); cross-service consistency; audit needs.",
      "Event sourcing vs CRUD state machine; Temporal for long-running workflows."),
    E("ecommerce", "Recommendation System",
      "Surface relevant products to lift engagement and revenue.",
      "Two-stage funnel: candidate generation (collaborative filtering / two-tower embeddings -> ANN, recall) then ranking (deep model on rich features, precision), plus business re-ranking (margin, stock, diversity). Train offline, serve online, evaluate via A/B.",
      "'Customers who bought X also bought Y' (item-item CF); personalized home via embeddings + ranker.",
      "Homepage, PDP related, cart cross-sell, email.",
      "Events -> train ; request -> candidate gen (ANN) -> ranker -> re-rank -> results",
      "Cold start; feedback loops/popularity bias; heavy infra; needs experimentation.",
      "CF, content-based, matrix factorization, two-tower; TF Recommenders, FAISS, AWS Personalize."),
    E("ecommerce", "Notification System",
      "Send billions of transactional and marketing messages reliably across channels.",
      "Async via queues: a service consumes events, renders templates, dispatches to channel providers with retries/rate limits; respects prefs/opt-outs; dedupes; tracks delivery; priority queues split transactional from marketing.",
      "OrderShipped -> queue -> worker renders email -> SES/Twilio/FCM; retries with backoff.",
      "Order updates, OTP, price-drop alerts, marketing campaigns.",
      "Events -> [Queue] -> Notification workers -> {Email, SMS, Push} + prefs/retry/dedupe",
      "Provider limits/failures; deliverability/spam; ordering; cost at scale.",
      "SES/SNS, Twilio, FCM/APNs, SendGrid; Kafka/SQS backbone."),
    E("ecommerce", "Flash Sale / High-Contention Drop",
      "Millions rush a few thousand units in seconds — extreme spike + last-item problem.",
      "Protect the system: waiting-room/queue to admit gradually; pre-warm caches; Redis atomic DECR for stock (stop at 0), reconcile to DB async; aggressive rate limiting + bot detection; idempotent add-to-cart/checkout; graceful degradation.",
      "Limited-drop: virtual queue -> buy token -> Redis atomic stock -> async order persistence.",
      "Flash sales, ticket/limited drops, Black Friday doorbusters.",
      "Users -> waiting room -> rate limiter -> Redis DECR -> token -> async order -> DB",
      "Redis-DB reconciliation; fairness vs bots; huge transient load; over-provisioning cost.",
      "Redis atomic/Lua, queue-based admission, CDN edge logic."),

    # ─────────── AI Solutions ───────────
    E("ai_solutions", "ML System Design (framework)",
      "You must turn a business problem into a production ML system, not just a model in a notebook.",
      "Follow a spine: clarify the objective & business metric -> frame as ML (target, task) -> data & features (with leakage checks) -> baseline then advanced model -> serving (batch vs real-time, the two-stage retrieve->rank funnel) -> evaluation (offline metrics + online A/B) -> monitoring & retraining.",
      "Design 'recommended for you': candidate generation (two-tower + ANN) -> ranking model -> re-rank; ship behind an A/B test on engagement.",
      "Any ML feature: recommendations, search ranking, fraud, forecasting, personalization.",
      "Data -> features -> train -> registry -> serve (retrieve->rank) -> monitor -> retrain",
      "Cold start; train/serve skew; data quality; feedback loops; needs experimentation infra.",
      "TensorFlow/PyTorch, SageMaker/Vertex, two-tower + FAISS, gradient boosting.",
      ["ml-system-design", "recommendation", "ranking", "mlops", "interview"]),
    E("ai_solutions", "MLOps & Model Lifecycle",
      "Models decay, are hard to reproduce, and 'works on my laptop' doesn't ship — you need engineering discipline around ML.",
      "Automate the lifecycle: versioned data + code + models, reproducible training pipelines, CI/CD for models (test, validate, canary), a model registry, automated deployment, and continuous monitoring that triggers retraining. Treat models as products with SLAs.",
      "A pipeline retrains nightly, validates against a holdout + guardrails, registers the model, and canary-deploys if metrics pass.",
      "Any production ML, regulated ML, teams shipping many models.",
      "Data/code versioned -> CI train+validate -> registry -> canary deploy -> monitor -> retrain",
      "Tooling complexity; pipeline maintenance; reproducibility is hard; org/culture change.",
      "MLflow, Kubeflow, SageMaker/Vertex Pipelines, DVC, Weights & Biases, Airflow/Dagster.",
      ["mlops", "ci-cd", "model-registry", "lifecycle", "reproducibility"]),
    E("ai_solutions", "Model Serving & Inference at Scale",
      "Serving predictions at high QPS with low latency (and GPU cost) is a distinct engineering problem from training.",
      "Choose batch (offline scoring) vs real-time (online) serving. Real-time: a model server behind an autoscaled service, with request batching, GPU/accelerator scheduling, caching of hot predictions, and A/B/shadow deployment. Optimize models (quantization, distillation, ONNX/TensorRT) for latency.",
      "A ranking model served via Triton on GPUs with dynamic batching; embeddings cached in Redis; canary + shadow traffic for new versions.",
      "Real-time recommendations, fraud scoring, search ranking, LLM inference.",
      "Request -> model server (batched) -> GPU -> prediction (+ cache, A/B, shadow)",
      "GPU cost; latency vs throughput (batching); cold starts; version management.",
      "Triton, TorchServe, TF Serving, KServe, Ray Serve, vLLM (LLMs), BentoML.",
      ["inference", "serving", "gpu", "latency", "scaling", "llm"]),
    E("ai_solutions", "RAG (Retrieval-Augmented Generation)",
      "LLMs hallucinate and don't know your private/current data, and retraining them on it is costly and slow.",
      "Ground the LLM in your data at query time: chunk & embed documents into a vector DB; on a query, embed it, retrieve the top-k relevant chunks (semantic search, often + keyword hybrid + a re-ranker), stuff them into the prompt as context, and let the LLM answer with citations. No model retraining.",
      "An internal support assistant: policy docs embedded in a vector DB; user question -> retrieve relevant passages -> LLM answers grounded in them with sources.",
      "Enterprise Q&A/chatbots, doc search, customer support, coding assistants, knowledge bases.",
      "Docs -> chunk -> embed -> [Vector DB]; Query -> embed -> retrieve top-k -> rerank -> LLM(context) -> answer",
      "Retrieval quality caps answer quality; chunking/embedding tuning; context-window limits; freshness; still can hallucinate.",
      "Fine-tuning (bakes knowledge in), long-context models, agentic retrieval; LangChain/LlamaIndex.",
      ["rag", "llm", "genai", "vector-db", "embeddings", "retrieval", "hallucination"]),
    E("ai_solutions", "Vector Database & ANN Search",
      "Finding semantically similar items among millions/billions of high-dimensional embeddings by brute force is too slow.",
      "Store embeddings and query by nearest-neighbor. Approximate Nearest Neighbor (ANN) indexes (HNSW graphs, IVF, PQ) trade a little recall for huge speed. Support metadata filtering, hybrid (vector+keyword) search, and updates.",
      "RAG retrieval and 'similar products'/'shop the look' powered by an HNSW index over product/text embeddings.",
      "RAG, semantic search, recommendations, image/similarity search, dedup, anomaly detection.",
      "Embeddings -> [ANN index (HNSW/IVF-PQ)] ; query vector -> top-k similar + metadata filter",
      "Approximate (recall<100%); index build/memory cost; freshness on updates; tuning recall vs latency.",
      "Pinecone, Weaviate, Milvus, Qdrant, pgvector, FAISS, Elasticsearch/OpenSearch kNN, Redis.",
      ["vector-db", "ann", "embeddings", "semantic-search", "hnsw", "rag", "faiss"]),
    E("ai_solutions", "LLM: Prompting vs Fine-tuning vs RAG",
      "Given a task, how do you make an LLM good at it — cheaply and maintainably?",
      "Decision ladder: (1) Prompt engineering / few-shot (cheapest, no training) — try first. (2) RAG when the model needs your private/fresh knowledge. (3) Fine-tuning (or PEFT/LoRA) when you need a specific style/format/behavior or lower latency/cost at scale. Often combine RAG + light fine-tuning.",
      "Support bot: RAG for the knowledge; a small LoRA fine-tune for tone/format; prompt for guardrails.",
      "Chatbots, extraction, classification, code, domain assistants.",
      "Prompt (cheap) -> RAG (needs data) -> Fine-tune/LoRA (needs behavior) -> combine",
      "Prompting is brittle; RAG depends on retrieval; fine-tuning needs data + MLOps and can go stale.",
      "OpenAI/Anthropic APIs, open models (Llama), LoRA/PEFT, RAG frameworks, distillation.",
      ["llm", "fine-tuning", "rag", "prompt-engineering", "lora", "genai"]),
    E("ai_solutions", "LLMOps, Prompt Management & Guardrails",
      "Production LLM apps need versioned prompts, safety, evals, and cost/latency control — not ad-hoc prompt strings in code.",
      "Manage prompts as versioned artifacts with A/B tests; add input/output guardrails (PII redaction, toxicity, jailbreak/prompt-injection defense, schema validation); evaluate quality with LLM-as-judge + human review + regression suites; observe cost, latency, and tokens; cache and route between models.",
      "A prompt registry + eval harness gates prompt changes; a guardrail layer blocks prompt-injection and validates JSON output before it reaches downstream systems.",
      "Any customer-facing GenAI feature, agents, RAG apps.",
      "User -> input guardrails -> prompt (versioned) -> LLM -> output guardrails/validate -> log/eval/cost",
      "Eval is hard (subjective); guardrails add latency; prompt-injection is an open problem; cost sprawl.",
      "LangSmith, Langfuse, PromptLayer, Guardrails AI, NeMo Guardrails, Ragas (eval).",
      ["llmops", "prompt", "guardrails", "eval", "safety", "prompt-injection", "genai"]),
    E("ai_solutions", "Agentic AI / Tool Use & Orchestration",
      "Some tasks need an LLM to plan multiple steps, call tools/APIs, and use memory — not answer in one shot.",
      "An agent loop: the LLM reasons, chooses a tool (function calling), executes it, observes the result, and iterates until done. Add planning, memory (short/long-term via vector store), multi-agent collaboration, and strict tool permissions + human-in-the-loop for risky actions.",
      "A 'research agent' that searches, reads pages, and synthesizes; or an ops agent that queries systems and drafts actions for approval.",
      "Research/automation assistants, workflow automation, coding agents, customer ops.",
      "LLM plan -> pick tool -> call API -> observe -> loop -> answer (+ memory, guardrails, human approval)",
      "Reliability/looping/cost; compounding errors; security of tool access; hard to evaluate; latency.",
      "LangGraph, AutoGPT-style, CrewAI, OpenAI/Anthropic tool-use, Temporal for durable orchestration.",
      ["agents", "agentic", "tool-use", "orchestration", "llm", "genai", "function-calling"]),
    E("ai_solutions", "Model Monitoring & Drift Detection",
      "Models silently degrade as the world changes (data/concept drift); you must catch it before it costs money.",
      "Monitor input feature distributions (data drift), prediction distributions, and — when labels arrive — live accuracy vs training. Detect drift (PSI, KS-test), alert, and trigger retraining. Also watch latency, errors, and business KPIs. Close the loop with a feedback pipeline.",
      "A fraud model's input drift alarm fires after a new attack pattern; retraining is triggered automatically.",
      "Any production ML, especially fraud, ranking, pricing, forecasting.",
      "Live features/preds -> drift checks (PSI/KS) + delayed labels -> alert -> retrain",
      "Label delay/absence; defining 'drift' thresholds; alert fatigue; ground-truth cost.",
      "Evidently, WhyLabs, Arize, Fiddler, SageMaker Model Monitor, custom Prometheus.",
      ["monitoring", "drift", "mlops", "observability", "retraining"]),
    E("ai_solutions", "Distributed Model Training (GPUs at scale)",
      "Large models/datasets don't fit or train fast enough on one GPU.",
      "Parallelize: data parallelism (replicate model, split batch, all-reduce gradients), model/tensor parallelism (split the model across GPUs), pipeline parallelism (split by layers). Use mixed precision, gradient checkpointing, and high-speed interconnect (NVLink/InfiniBand). Orchestrate on GPU clusters.",
      "Training a large model with data + tensor parallelism across a multi-node GPU cluster using DeepSpeed/FSDP.",
      "LLM/foundation-model training, large-scale deep learning, fine-tuning at scale.",
      "Batch split -> {GPU1..GPUn replicas} -> all-reduce gradients; big model -> tensor/pipeline split",
      "Cost; interconnect bottlenecks; complexity; fault tolerance over long runs; scheduling.",
      "PyTorch DDP/FSDP, DeepSpeed, Megatron-LM, Horovod, Ray Train, JAX; Slurm/Kubernetes.",
      ["training", "distributed", "gpu", "data-parallel", "deepspeed", "scaling"]),
    E("ai_solutions", "Responsible AI (Fairness, Explainability, Governance)",
      "AI systems can be biased, opaque, and non-compliant — a real business, legal, and ethical risk.",
      "Bake in: bias/fairness testing across protected groups, explainability (SHAP/LIME, model cards), human oversight for high-stakes decisions, privacy (PII handling, differential privacy), and governance (documentation, audit trails, approval gates, regulatory alignment).",
      "A credit/hiring model is fairness-tested per group, explained with SHAP, documented in a model card, and human-reviewed before decisions.",
      "Regulated domains (credit, hiring, health), any high-impact AI, enterprise AI governance.",
      "Model -> fairness tests + explainability + model card + human oversight + audit trail",
      "Fairness definitions conflict; explainability is approximate; overhead vs speed; evolving regulation.",
      "SHAP, LIME, Fairlearn, AIF360, model cards; governance platforms; upcoming AI regulations.",
      ["responsible-ai", "fairness", "bias", "explainability", "governance", "ethics", "compliance"]),
    E("ai_solutions", "LLM Cost & Latency Optimization",
      "LLM tokens and GPU time are expensive; naive usage is slow and burns budget.",
      "Levers: prompt caching + semantic response caching; model routing (small model for easy queries, big for hard); distillation/quantization to smaller/faster models; batching; streaming responses; trimming context (better retrieval, summarization); and rate/budget controls.",
      "A router sends simple FAQs to a cheap small model and hard queries to a frontier model; frequent answers are cache-served.",
      "High-volume GenAI apps, chatbots, agents, RAG at scale.",
      "Query -> cache? -> router (small vs large) -> (batched/streamed) -> response; trim context",
      "Cache invalidation; routing errors hurt quality; quantization can drop accuracy; complexity.",
      "Semantic cache (GPTCache), model routing, quantization (GPTQ/AWQ), distillation, vLLM batching.",
      ["llm", "cost", "latency", "caching", "quantization", "routing", "optimization"]),
    E("ai_solutions", "Data Labeling & Annotation Pipeline",
      "Supervised ML needs high-quality labeled data at scale, which is costly and error-prone.",
      "Build a labeling pipeline: guidelines, a labeling tool/workforce, quality control (consensus, gold tasks, inter-annotator agreement), active learning (label the most informative samples first), and weak/auto-labeling to bootstrap. Feed labels back into training.",
      "An active-learning loop surfaces uncertain images for human labeling; consensus + gold checks ensure quality before training.",
      "Computer vision, NLP, any supervised learning, RLHF data for LLMs.",
      "Unlabeled -> active learning (pick informative) -> annotators + QC -> labeled -> train -> repeat",
      "Cost & time; label noise/bias; guideline drift; scaling human review.",
      "Label Studio, Scale AI, Snorkel (weak supervision), Amazon SageMaker Ground Truth.",
      ["data-labeling", "annotation", "active-learning", "supervised", "rlhf", "data-quality"]),

    # ─────────── Information Security ───────────
    E("infosec", "Threat Modeling (STRIDE)",
      "You can't secure a system without systematically identifying what can go wrong before attackers do.",
      "Model the system (data-flow diagram, trust boundaries) and enumerate threats with STRIDE: Spoofing, Tampering, Repudiation, Information disclosure, Denial of service, Elevation of privilege. For each, decide mitigate/accept/transfer/eliminate, and track as security requirements.",
      "Before launching a payments service, a threat-modeling session maps trust boundaries and drives controls (mTLS, input validation, audit logs, rate limits).",
      "Design reviews, new features, security architecture, secure SDLC.",
      "DFD + trust boundaries -> STRIDE per element -> rank -> mitigations -> track",
      "Time-consuming; needs security expertise; can miss novel threats; must be kept current.",
      "STRIDE, PASTA, LINDDUN (privacy), attack trees; tools: Microsoft Threat Modeling Tool, IriusRisk.",
      ["threat-modeling", "stride", "secure-sdlc", "risk", "design-review"]),
    E("infosec", "Zero Trust Architecture",
      "Perimeter security fails once inside; a breached network shouldn't mean full access ('castle-and-moat' is dead).",
      "'Never trust, always verify': authenticate and authorize every request based on identity, device posture, and context — regardless of network location. Enforce least privilege, micro-segmentation, continuous verification, and encryption everywhere. Access is per-resource, policy-driven.",
      "An employee accessing an internal app is verified by identity + device health + context on every request via an identity-aware proxy, not by being 'on the VPN'.",
      "Modern enterprise access, remote work, cloud, reducing lateral movement.",
      "User/device -> identity-aware proxy (verify identity+posture+context) -> per-resource least-privilege access",
      "Big architectural/cultural shift; legacy apps hard to fit; policy complexity; identity is now the critical dependency.",
      "BeyondCorp (Google), Zscaler, Cloudflare Access, Azure AD Conditional Access; vs VPN/perimeter.",
      ["zero-trust", "access", "least-privilege", "identity", "network-security", "beyondcorp"]),
    E("infosec", "IAM — Identity & Access Management",
      "Controlling who can access what, at scale, without over-permissioning or unmanaged accounts.",
      "Separate AuthN (who you are) from AuthZ (what you can do). Manage identities (users, services), enforce least privilege via RBAC (roles) or ABAC (attributes/policies), require MFA, and lifecycle accounts (joiner/mover/leaver). Centralize with an identity provider + SSO. Audit access regularly.",
      "A cloud org uses SSO + MFA, IAM roles with least-privilege policies, and quarterly access reviews to remove stale permissions.",
      "Enterprise access, cloud (AWS/Azure IAM), microservice-to-service auth, compliance.",
      "Identity provider -> AuthN (MFA) -> AuthZ (RBAC/ABAC least-privilege) -> audit/review",
      "Role explosion / privilege creep; orphaned accounts; complexity; balancing security vs friction.",
      "Okta, Azure AD/Entra, AWS IAM, Ping, Keycloak; RBAC vs ABAC; PAM for privileged.",
      ["iam", "authentication", "authorization", "rbac", "abac", "mfa", "sso", "least-privilege"]),
    E("infosec", "OAuth 2.0 / OIDC / SSO",
      "Apps need delegated authorization and single sign-on without sharing passwords across services.",
      "OAuth 2.0 delegates authorization via access tokens (scopes) so app A can act on a user's behalf at app B without their password. OIDC adds an identity layer (ID token/JWT) for authentication. SSO lets one login grant access to many apps. Use the authorization-code + PKCE flow for web/mobile.",
      "'Sign in with Google' (OIDC) + an API using OAuth scopes; enterprise SSO via SAML/OIDC to all internal tools.",
      "Third-party login, API authorization, enterprise SSO, mobile/SPA auth.",
      "User -> Auth server -> code (PKCE) -> access token (scopes) + ID token (JWT) -> resource server",
      "Token leakage/misuse; complex flows done wrong; JWT revocation is hard; scope creep.",
      "OAuth2/OIDC (Okta, Auth0, Keycloak), SAML (legacy SSO); JWT vs opaque tokens.",
      ["oauth", "oidc", "sso", "jwt", "authentication", "authorization", "saml", "identity"]),
    E("infosec", "Encryption (At Rest, In Transit, KMS)",
      "Data must stay confidential even if disks, backups, or network traffic are captured.",
      "Encrypt in transit (TLS/mTLS) and at rest (disk/DB/field-level). Use a Key Management Service and envelope encryption (a data key encrypts data; a master key in the KMS/HSM encrypts the data key), with rotation, access control, and audit. For sensitive fields, application-level/field encryption or tokenization.",
      "Card data is tokenized; DB is encrypted at rest with KMS-managed keys; all service traffic uses mTLS.",
      "PII/payment data, compliance (PCI/GDPR/HIPAA), multi-tenant isolation.",
      "Data -> data key (envelope) -> encrypted; data key -> master key in KMS/HSM (rotated, audited); TLS/mTLS in transit",
      "Key management complexity; performance overhead; lost keys = lost data; encryption != access control.",
      "AWS KMS/CloudHSM, HashiCorp Vault, TLS/mTLS, tokenization; AES-256, envelope encryption.",
      ["encryption", "tls", "mtls", "kms", "envelope-encryption", "key-management", "data-protection", "pci"]),
    E("infosec", "Secrets Management",
      "Hardcoded passwords/API keys in code or config leak constantly and are a top breach cause.",
      "Store secrets in a dedicated vault with encryption, fine-grained access control, dynamic short-lived credentials, automatic rotation, and full audit. Apps fetch secrets at runtime (never commit them); scan repos for leaked secrets.",
      "Services fetch DB credentials from Vault at startup with short-lived, auto-rotated leases; CI scans block committed keys.",
      "App credentials, DB passwords, API keys, certificates, cloud access.",
      "App -> [Vault] fetch short-lived secret (rotated, audited); repo secret-scanning in CI",
      "Vault becomes critical infra/SPOF; rotation can break apps; bootstrapping the first secret; sprawl.",
      "HashiCorp Vault, AWS Secrets Manager, Azure Key Vault, GCP Secret Manager; git-secrets/TruffleHog.",
      ["secrets", "vault", "credentials", "rotation", "key-management", "devsecops"]),
    E("infosec", "Network Security (Segmentation, Firewall, WAF, DDoS)",
      "Flat networks let one compromise spread everywhere, and public endpoints get attacked constantly.",
      "Layer defenses: segment networks (VPCs/subnets/security groups) to limit blast radius; firewalls control traffic; a WAF filters app-layer attacks (SQLi/XSS); DDoS protection absorbs volumetric floods; private endpoints and bastion/zero-trust for internal access.",
      "Public tier -> WAF + DDoS at the edge; app tier in private subnets; DB tier reachable only from the app tier via security groups.",
      "Any internet-facing system, multi-tier apps, cloud networks.",
      "Internet -> DDoS/WAF -> LB -> [public subnet] -> [private app subnet] -> [private data subnet]",
      "Misconfiguration is common and dangerous; over-segmentation adds complexity; WAF false positives.",
      "AWS Security Groups/NACLs, Cloudflare/Akamai (WAF+DDoS), Palo Alto, AWS Shield, service mesh mTLS.",
      ["network-security", "firewall", "waf", "ddos", "segmentation", "vpc", "perimeter"]),
    E("infosec", "Application Security & OWASP Top 10",
      "Most breaches exploit application vulnerabilities (injection, broken auth, misconfig), not the network.",
      "Defend against the OWASP Top 10: input validation & parameterized queries (injection), strong auth/session management, access control checks, secure defaults, output encoding (XSS), dependency management (known-vuln components), logging. Bake security into the SDLC (secure coding, reviews, testing).",
      "Parameterized queries + an ORM stop SQLi; output encoding stops XSS; server-side authorization checks stop IDOR/broken access control.",
      "All web/mobile/API apps, secure SDLC.",
      "Input -> validate -> parameterized query/ORM -> authz check -> output encode -> log",
      "Requires developer security awareness; new vuln classes emerge; frameworks help but aren't foolproof.",
      "OWASP Top 10/ASVS, SAST/DAST/SCA tools, secure frameworks, security code review.",
      ["appsec", "owasp", "sqli", "xss", "injection", "secure-coding", "vulnerability", "web-security"]),
    E("infosec", "API Security",
      "APIs are the primary attack surface for modern apps and have their own OWASP Top 10.",
      "Authenticate every call (OAuth/JWT/API keys), authorize per object (stop BOLA/IDOR — check the user owns the resource), validate & rate-limit inputs, avoid excessive data exposure, use TLS, and put an API gateway in front for centralized auth, throttling, and schema validation.",
      "A gateway validates JWTs and rate-limits; each endpoint verifies the caller owns the requested object id (prevents IDOR).",
      "Microservices, mobile/SPA backends, partner/public APIs.",
      "Client -> API Gateway (authn, rate-limit, schema) -> service (per-object authz) -> TLS everywhere",
      "Object-level authz is easy to miss (BOLA is #1); versioning; shadow/zombie APIs; token handling.",
      "OWASP API Top 10, API gateways (Kong/Apigee), OAuth2/JWT, mTLS, schema validation.",
      ["api-security", "bola", "idor", "jwt", "rate-limiting", "gateway", "owasp"]),
    E("infosec", "PKI, Certificates & mTLS",
      "Services and users need to prove identity and communicate securely without pre-shared secrets.",
      "Public Key Infrastructure issues digital certificates (via a Certificate Authority) binding identities to public keys. TLS uses them for server (and with mTLS, client) authentication + encryption. Manage the cert lifecycle: issuance, rotation, revocation (CRL/OCSP), and automation to avoid outages from expiry.",
      "Service-to-service calls use mTLS with short-lived certs auto-issued/rotated by a service mesh; public sites use CA-signed TLS certs auto-renewed.",
      "TLS/HTTPS, service-to-service auth (mesh), device identity, code signing, email.",
      "CA -> issues cert (identity+public key) -> TLS/mTLS handshake -> encrypted+authenticated; rotate/revoke",
      "Expiry causes outages; revocation is weak (OCSP/CRL); CA compromise is catastrophic; key protection.",
      "Let's Encrypt/ACME, internal CA (Vault PKI), service mesh (Istio/Linkerd) mTLS, cert-manager.",
      ["pki", "certificates", "tls", "mtls", "ca", "encryption", "service-mesh"]),
    E("infosec", "SIEM, SOC & Detection/Response",
      "You need to detect and respond to attacks across a sprawling estate before they cause damage.",
      "A SIEM centralizes logs/events, correlates them into alerts (rules + anomaly/ML detection), and feeds a SOC team + SOAR for automated response. Map detections to MITRE ATT&CK; tune to cut noise; measure MTTD/MTTR. Add EDR/XDR telemetry from endpoints.",
      "Failed-login spikes + unusual data egress correlate into an alert; SOAR auto-isolates the host and opens an incident.",
      "Enterprise security operations, threat detection, compliance monitoring, incident response.",
      "Logs/EDR/cloud -> [SIEM correlation + detections] -> SOC + SOAR (auto-respond) -> investigate",
      "Alert fatigue/false positives; log volume & cost; skilled analysts scarce; detection engineering effort.",
      "Splunk, Microsoft Sentinel, Elastic SIEM, Chronicle, QRadar; MITRE ATT&CK, SOAR, EDR/XDR.",
      ["siem", "soc", "detection", "incident-response", "mitre-attack", "soar", "logging", "monitoring"]),
    E("infosec", "DevSecOps (Security in CI/CD)",
      "Security bolted on at the end is slow and misses issues; it must be automated into the pipeline ('shift left').",
      "Embed automated security in CI/CD: SAST (code), SCA (dependencies/CVEs), secret scanning, DAST (running app), IaC scanning, and container image scanning — failing builds on critical findings. Add signed artifacts and supply-chain security (SBOM, provenance).",
      "Every PR runs SAST + dependency + secret scans; container images are scanned and signed; IaC is checked before apply.",
      "Modern software delivery, cloud-native, regulated pipelines.",
      "Commit -> SAST + SCA + secret scan -> build -> image scan + sign -> DAST -> deploy (gates)",
      "False positives slow devs; tool sprawl; culture change; can't catch everything; supply-chain is hard.",
      "Snyk, SonarQube, Semgrep, Trivy, OWASP ZAP, Checkov, Sigstore/SBOM; vs manual pentest.",
      ["devsecops", "sast", "dast", "sca", "ci-cd", "supply-chain", "shift-left", "container-security"]),
    E("infosec", "Cloud Security & Shared Responsibility",
      "Cloud misconfiguration (public buckets, over-permissive IAM) is a leading breach cause; responsibility is split.",
      "Understand the shared-responsibility model (provider secures the cloud; you secure what's in it). Enforce least-privilege IAM, encrypt data, lock down network/storage, and use CSPM to continuously detect misconfigurations and drift. Centralize logging (CloudTrail) and guardrails (SCPs/policies).",
      "CSPM flags a public S3 bucket and an over-permissive IAM role; guardrails prevent creating them in the first place.",
      "Any cloud/multi-cloud estate, compliance, preventing data exposure.",
      "Account guardrails (SCP) + least-priv IAM + encryption + CSPM (detect misconfig) + central logging",
      "Complexity/multi-cloud; config drift; identity sprawl; misconfig is easy; shared-responsibility confusion.",
      "AWS Security Hub/GuardDuty, Wiz, Prisma Cloud, CSPM/CNAPP tools, cloud-native IAM.",
      ["cloud-security", "cspm", "misconfiguration", "iam", "shared-responsibility", "guardrails", "cnapp"]),
    E("infosec", "Container & Kubernetes Security",
      "Containers and K8s add new attack surfaces: vulnerable images, over-privileged pods, exposed APIs.",
      "Secure the pipeline and runtime: scan images for CVEs, use minimal/distroless base images, sign images, drop root/capabilities, apply pod security standards & network policies, lock down the K8s API/RBAC, manage secrets properly, and add runtime threat detection.",
      "Admission control blocks unsigned or high-CVE images; network policies isolate namespaces; runtime sensors flag anomalous container behavior.",
      "Kubernetes/container platforms, cloud-native apps, microservices.",
      "Scan+sign image -> admission control -> least-priv pod (no root) + network policy + RBAC -> runtime detection",
      "Complexity; fast-moving ecosystem; default configs are insecure; runtime security is immature vs traditional.",
      "Trivy, Falco, OPA/Gatekeeper, Kyverno, Aqua/Sysdig, pod security standards, service mesh mTLS.",
      ["container-security", "kubernetes", "k8s", "image-scanning", "runtime-security", "rbac", "cloud-native"]),
    E("infosec", "Data Security, DLP & Classification",
      "You must know where sensitive data lives and prevent it from leaking or being misused.",
      "Classify data by sensitivity (public/internal/confidential/regulated), discover it across stores, and enforce controls: encryption, access control, masking/tokenization, and Data Loss Prevention to detect/block exfiltration (email, uploads, endpoints). Add retention/deletion for privacy compliance.",
      "DLP blocks an attempt to email a file containing card numbers; sensitive columns are masked for analysts; PII is auto-discovered and tagged.",
      "PII/PHI/PCI protection, GDPR/HIPAA compliance, insider-threat, exfiltration prevention.",
      "Discover + classify data -> controls (encrypt/mask/tokenize/access) -> DLP monitors egress -> retention",
      "Classification at scale is hard; DLP false positives; performance; balancing usability; shadow data.",
      "Microsoft Purview, DLP tools, tokenization, data discovery/classification, masking.",
      ["data-security", "dlp", "classification", "pii", "masking", "tokenization", "gdpr", "privacy"]),
    E("infosec", "Privileged Access Management (PAM)",
      "Admin/root credentials are the crown jewels; their misuse or theft causes the worst breaches.",
      "Control privileged accounts: vault and rotate credentials, grant just-in-time/just-enough access (no standing admin), require approval + MFA, broker sessions through a proxy, and record/audit every privileged session. Eliminate shared admin accounts.",
      "An engineer requests time-boxed prod DB admin; it's approved, brokered with MFA, session-recorded, and auto-revoked after the window.",
      "Admin access, DevOps, third-party/vendor access, compliance.",
      "Request -> approval + MFA -> JIT elevated access (brokered, recorded) -> auto-revoke -> audit",
      "Friction for admins; adoption resistance; break-glass handling; PAM itself is a high-value target.",
      "CyberArk, BeyondTrust, HashiCorp Boundary, AWS IAM (temporary creds), Teleport.",
      ["pam", "privileged-access", "jit", "least-privilege", "credentials", "audit", "iam"]),
    E("infosec", "Incident Response & Forensics",
      "Breaches are inevitable; how fast and well you respond determines the damage.",
      "Follow a plan (NIST): Prepare, Detect & Analyze, Contain, Eradicate, Recover, Post-incident lessons. Define roles, runbooks, and comms; preserve forensic evidence (chain of custody); contain to stop spread; eradicate the root cause; recover safely; then do a blameless retro and fix systemic gaps.",
      "On detecting ransomware, the team isolates hosts, preserves evidence, eradicates persistence, restores from clean backups, and runs a post-incident review.",
      "Security operations, breach response, ransomware, regulatory notification.",
      "Prepare -> Detect/Analyze -> Contain -> Eradicate -> Recover -> Post-incident review",
      "Chaos under pressure; evidence handling; communication/legal duties; needs practice (tabletops).",
      "NIST IR / SANS PICERL, SOAR, EDR/forensics tools, runbooks, tabletop exercises.",
      ["incident-response", "forensics", "ransomware", "nist", "containment", "soc", "breach"]),
    E("infosec", "Compliance & Security Frameworks",
      "Enterprises must prove security to customers and regulators, and align controls to recognized standards.",
      "Map controls to frameworks: SOC 2 (trust criteria for SaaS), ISO 27001 (ISMS), PCI-DSS (cards), GDPR/DPDP (privacy), HIPAA (health), NIST CSF (risk). Do risk assessments, implement controls, collect evidence continuously, and pass audits. Treat compliance as a byproduct of real security, not the goal.",
      "A SaaS pursues SOC 2 Type II: define controls, automate evidence collection, run continuous monitoring, and pass the external audit.",
      "SaaS sales/enterprise trust, regulated industries, data privacy, vendor risk.",
      "Framework -> risk assessment -> controls -> continuous evidence -> audit -> attestation",
      "Checkbox-compliance != security; audit cost/effort; overlapping frameworks; keeping evidence current.",
      "SOC 2, ISO 27001, PCI-DSS, GDPR/DPDP, HIPAA, NIST CSF; Vanta/Drata (automation).",
      ["compliance", "soc2", "iso27001", "pci-dss", "gdpr", "hipaa", "nist", "governance", "audit"]),
    E("infosec", "Security for AI/ML (Adversarial & LLM Threats)",
      "AI systems have unique attack surfaces — poisoned data, evasion, model theft, and prompt injection — that classic security misses. (Bridges the AI + Infosec charters.)",
      "Defend the ML lifecycle: data-poisoning defenses & provenance, robustness to adversarial/evasion inputs, model-theft/extraction protection, privacy (membership-inference/DP), and for LLMs specifically: prompt-injection & jailbreak defenses, output filtering, tool-permission scoping, and guarding against sensitive-data leakage in RAG. Map to OWASP LLM Top 10 / MITRE ATLAS.",
      "A RAG assistant sanitizes retrieved content and user input to block prompt injection, scopes tool access, and filters outputs to prevent data exfiltration.",
      "Any production AI, especially LLM/agentic apps, fraud/security ML, high-stakes models.",
      "Data (provenance) -> robust training -> access-controlled model -> input/output guardrails (prompt-injection) -> monitor",
      "Fast-evolving threats; prompt injection largely unsolved; robustness vs accuracy trade-offs; hard to test.",
      "OWASP LLM Top 10, MITRE ATLAS, adversarial-robustness tooling, guardrails, model watermarking.",
      ["ai-security", "adversarial", "prompt-injection", "llm", "model-theft", "owasp-llm", "mitre-atlas", "genai", "ml"]),
    E("infosec", "Backup, Ransomware Resilience & DR",
      "Ransomware and disasters can wipe out data and operations; resilience is a security requirement.",
      "Apply 3-2-1 backups (3 copies, 2 media, 1 offsite), with immutable/air-gapped copies ransomware can't encrypt, tested restores, and defined RPO/RTO. Pair with a DR plan (failover, runbooks) and regular recovery drills. Segment backups from production credentials.",
      "Immutable daily backups + a tested restore runbook let a company recover from ransomware without paying, meeting its RTO.",
      "Ransomware defense, disaster recovery, business continuity, compliance.",
      "Prod -> 3-2-1 backups (immutable/air-gapped) -> tested restores -> DR failover (RPO/RTO)",
      "Backup cost/complexity; untested backups fail when needed; backup systems are attack targets; RTO gaps.",
      "Immutable object storage (S3 Object Lock), Veeam, cloud DR, air-gapped/offline backups; BCP-DR.",
      ["backup", "ransomware", "disaster-recovery", "dr", "resilience", "rpo-rto", "business-continuity"]),

    # ─────────── Classic Design Problems (Alex Xu style) ───────────
    E("classic", "Design a URL Shortener (TinyURL)",
      "Map long URLs to short unique aliases and redirect, at read-heavy scale (billions of URLs, ~100:1 read:write).",
      "Generate a short key: base62-encode an auto-increment/Snowflake ID (no collisions) or hash+dedupe. Store key->long URL in a KV/DB; cache hot keys; 301/302 redirect on read. Estimate: 100M writes/day -> ~7 chars base62 covers 62^7 ≈ 3.5T. Read path is a single cache/DB lookup.",
      "bit.ly: write -> generate base62 key from a unique ID -> store; read -> cache -> DB -> HTTP redirect.",
      "Link shortening, tracking/analytics links, QR codes, campaign URLs.",
      "Create: long URL -> ID gen -> base62 -> store(KV) ; Read: short -> cache -> DB -> 301 redirect",
      "Key exhaustion planning; custom aliases; analytics write load; cache stampede on viral links.",
      "Base62 vs hash(MD5)+dedupe; KV (DynamoDB/Redis) vs SQL; 301 (cached) vs 302 (trackable).",
      ["url-shortener", "base62", "tinyurl", "kv-store", "redirect", "interview"],
      detail=_DETAIL_URL),
    E("classic", "Design a Unique ID Generator (Snowflake)",
      "Generate globally-unique, roughly time-sortable 64-bit IDs across many servers without a central bottleneck.",
      "Twitter Snowflake: a 64-bit ID = timestamp (41 bits, ms) + machine/datacenter id (10 bits) + per-ms sequence (12 bits). Each node generates locally (no coordination), IDs are k-sortable by time, and 4096 IDs/ms/node. Clock skew handled by waiting.",
      "Snowflake/Sonyflake generate order/message IDs that sort by creation time without a DB sequence.",
      "Primary keys at scale, ordering events, sharded systems needing unique IDs.",
      "64-bit = [ timestamp 41b | machineId 10b | sequence 12b ]  (generated locally per node)",
      "Clock drift/backwards-time; machine-id assignment; not cryptographically random; 69-year epoch limit.",
      "UUID (random, not sortable), DB auto-increment (bottleneck), ULID/KSUID, Snowflake variants.",
      ["unique-id", "snowflake", "distributed-id", "kv-store", "ordering"],
      detail=_DETAIL_UID),
    E("classic", "Design a Distributed Key-Value Store (Dynamo)",
      "Store huge KV data with high availability, horizontal scale, and tunable consistency across nodes.",
      "Dynamo-style: partition by consistent hashing onto a ring; replicate to N nodes; leaderless with quorum reads/writes (W+R>N) for tunable consistency; vector clocks / last-write-wins for conflicts; gossip for membership; Merkle trees for anti-entropy repair; hinted handoff for temporary failures.",
      "DynamoDB/Cassandra: writes go to N replicas on the ring; reads gather a quorum; nodes gossip health.",
      "Session stores, shopping carts, catalogs, any large highly-available KV workload.",
      "Key -> consistent-hash ring -> N replicas ; quorum W+R>N ; gossip + Merkle-tree repair",
      "Eventual consistency & conflict resolution; no complex queries/joins; tombstones; operational tuning.",
      "DynamoDB, Cassandra, Riak, Voldemort; vs strongly-consistent (Spanner) or single-node (Redis).",
      ["kv-store", "dynamo", "consistent-hashing", "quorum", "vector-clock", "gossip", "merkle-tree"],
      detail=_DETAIL_KV),
    E("classic", "Design a Web Crawler",
      "Crawl billions of web pages politely, freshly, and without loops, at scale.",
      "Seed URLs -> URL frontier (prioritized + politeness queues per host) -> fetcher (respect robots.txt, rate-limit per domain) -> parser -> content dedup (checksum/simhash) -> URL extractor -> URL dedup (Bloom filter / seen-set) -> back to frontier. Store pages in object storage; distributed workers.",
      "Search-engine or price crawler: distributed frontier + dedup ensures coverage, freshness, and no re-crawl storms.",
      "Search indexing, price monitoring, data collection, archival (Wayback), ML training corpora.",
      "Seeds -> [URL Frontier (priority+politeness)] -> Fetcher(robots) -> Parser -> dedup(Bloom) -> extract URLs -> Frontier",
      "Traps/infinite spaces; politeness vs speed; freshness vs cost; dynamic/JS pages; scale of dedup.",
      "Bloom filter for seen-URLs; BFS frontier; Nutch/Scrapy; simhash for near-dup content.",
      ["web-crawler", "url-frontier", "bloom-filter", "robots-txt", "dedup", "simhash"],
      detail=_DETAIL_CRAWLER),
    E("classic", "Design a News Feed / Timeline",
      "Build and deliver a personalized, ranked feed of posts from followed accounts at social scale.",
      "Two strategies: fan-out-on-write (push) precomputes each user's feed into a cache on post (fast reads, expensive for celebrities); fan-out-on-read (pull) assembles the feed at read time (cheap writes, slow reads). Hybrid: push for normal users, pull for high-follower accounts. Rank by an ML model; cache aggressively.",
      "Twitter/Instagram: a normal user's followers get the post pushed to their feed cache; a celebrity's posts are pulled at read time.",
      "Social feeds, activity streams, notifications, home timelines.",
      "Post -> fan-out-on-write -> follower feed caches ; celebrity -> fan-out-on-read merge ; rank -> serve",
      "Celebrity fan-out explosion; feed staleness vs cost; ranking complexity; storage for precomputed feeds.",
      "Push vs pull vs hybrid; Redis feed cache; ranking model; vs chronological.",
      ["news-feed", "timeline", "fan-out", "social", "ranking", "cache"],
      detail=_DETAIL_FEED),
    E("classic", "Design a Chat System (WhatsApp/Messenger)",
      "Deliver 1:1 and group messages in real time with presence, ordering, and offline delivery at billions-of-messages scale.",
      "Clients hold a persistent connection (WebSocket) to chat servers via a load balancer; a service discovery/session store maps user->server. Messages route through the server, persist to a message store (sharded by user/chat), and push to online recipients; offline users get a push notification and sync on reconnect. Presence via heartbeats.",
      "WhatsApp: WebSocket connection; message stored + delivered with ack; sequence IDs order messages; offline -> APNs/FCM push.",
      "Messaging apps, in-app chat, support chat, collaboration tools.",
      "Client <-WebSocket-> Chat server <-> Message store (sharded) ; offline -> push ; presence via heartbeat",
      "Connection state at scale; ordering & exactly-once delivery; group fan-out; presence cost; E2E encryption.",
      "WebSocket vs long-polling; message queue per user; Snowflake sequence IDs; Signal protocol (E2EE).",
      ["chat", "websocket", "messaging", "presence", "real-time", "push-notification"],
      detail=_DETAIL_CHAT),
    E("classic", "Design Search Autocomplete (Typeahead)",
      "Suggest top completions as the user types, within tens of milliseconds, over a huge query corpus.",
      "Build a Trie of popular queries annotated with frequencies; precompute top-k completions per prefix node (or cache them) so a lookup is O(prefix length). Update frequencies from query logs via a batch/stream pipeline. Serve from an in-memory/Redis structure; debounce client requests.",
      "Google/e-commerce search bar: prefix -> Trie/Redis top-k lookup returns ranked suggestions in <100ms.",
      "Search bars, command palettes, address/email autocomplete.",
      "Query logs -> aggregate -> [Trie with top-k per prefix] (in memory/Redis) ; keystroke -> prefix -> top-k",
      "Trie memory at scale; freshness (trending) vs precompute; personalization; typo tolerance.",
      "Trie + top-k cache; Elasticsearch completion suggester; vs full search; edge n-grams.",
      ["autocomplete", "typeahead", "trie", "top-k", "search", "prefix"],
      detail=_DETAIL_AUTOCOMPLETE),
    E("classic", "Design Video Streaming (YouTube/Netflix)",
      "Ingest, store, transcode, and stream video to billions with low buffering worldwide.",
      "Upload -> object storage -> transcoding pipeline produces multiple resolutions/bitrates + chunks (HLS/DASH). Metadata in a DB; video chunks + thumbnails served from a CDN close to users. Client uses adaptive bitrate streaming (switches quality by bandwidth). Recommendations + view counts via separate pipelines.",
      "YouTube: uploaded video is transcoded into renditions, chunked, pushed to CDN edges; players stream adaptively via HLS.",
      "Video platforms, live streaming, e-learning, media delivery.",
      "Upload -> S3 -> [Transcode -> renditions/chunks] -> CDN edges ; player -> adaptive bitrate (HLS/DASH)",
      "Transcoding cost/latency; storage of many renditions; CDN cost; live vs VOD; DRM.",
      "HLS/DASH adaptive streaming; CDN (CloudFront/Akamai); transcoding (FFmpeg/MediaConvert).",
      ["video-streaming", "youtube", "transcoding", "cdn", "hls", "adaptive-bitrate"]),
    E("classic", "Design Google Drive / File Sync",
      "Store, sync, and share files across devices with consistency, versioning, and efficient bandwidth.",
      "Files split into blocks (chunks); only changed blocks upload/download (delta sync). Metadata service tracks files, versions, block manifests; blocks in object storage (deduped by hash). A sync/notification service pushes changes to clients; conflicts resolved by versioning. Client watches the local folder and reconciles.",
      "Dropbox/Google Drive: edit a file -> only changed blocks sync; other devices get a change notification and pull deltas.",
      "File storage/sync, backup, collaboration, document sharing.",
      "Client (block delta) <-> Metadata service + Block store (deduped) <-> notification -> other devices",
      "Conflict resolution; large-file sync; dedup vs privacy; consistency across devices; offline edits.",
      "Block-level delta sync; content-addressed storage; vs naive full-file sync.",
      ["file-sync", "google-drive", "dropbox", "block-storage", "dedup", "delta-sync", "versioning"]),
    E("classic", "Design a Distributed Message Queue (Kafka)",
      "Move high-throughput event streams between producers and consumers, durably, ordered, and replayable.",
      "An append-only, partitioned commit log: topics split into partitions (ordering within a partition), each replicated across brokers. Producers append; consumers track their own offset and pull; consumer groups parallelize across partitions. Retention lets you replay. Partition key controls ordering + parallelism.",
      "Kafka: clickstream events partitioned by user id; multiple consumer groups (analytics, fraud, search) each read at their own offset.",
      "Event streaming, decoupling services, log aggregation, CDC, stream processing backbone.",
      "Producers -> [Topic: partitions (replicated log)] -> consumer groups (own offsets, pull)",
      "Ordering only within a partition; rebalancing pauses; exactly-once is hard; ops (ZK/KRaft); hot partitions.",
      "Kafka, Pulsar, Kinesis, RabbitMQ (broker model), Redis Streams; log vs queue semantics.",
      ["message-queue", "kafka", "commit-log", "partition", "offset", "streaming", "pub-sub"],
      detail=_DETAIL_MQ),
    E("classic", "Design Metrics Monitoring & Alerting",
      "Collect, store, and query billions of time-series metrics and alert on them in near-real-time.",
      "Agents/exporters emit metrics -> ingestion -> a time-series DB (columnar, downsampled, retention tiers) -> query/dashboards + an alerting engine evaluating rules against thresholds/anomalies -> notification channels. Use pull (Prometheus scrape) or push; aggregate at write for cardinality control.",
      "Prometheus scrapes services; Grafana dashboards; Alertmanager fires PagerDuty on SLO breach.",
      "Infra/app monitoring, SLO/SLA tracking, capacity planning, incident detection.",
      "Sources -> ingest -> [Time-series DB (downsample/retention)] -> dashboards + alert engine -> notify",
      "Cardinality explosion; storage/retention cost; alert fatigue/false positives; scaling the TSDB.",
      "Prometheus, InfluxDB, TimescaleDB, M3/Cortex/Thanos, Datadog; pull vs push.",
      ["monitoring", "metrics", "time-series", "alerting", "prometheus", "observability"]),
    E("classic", "Design Ad Click Event Aggregation",
      "Aggregate a massive stream of ad-click events into real-time counts (for billing/dashboards) accurately.",
      "Clicks -> stream (Kafka) -> stream processor (Flink) with windowed aggregation by ad/minute -> aggregated store (OLAP) -> dashboards/billing. Handle exactly-once (idempotent writes/dedup by event id), late/out-of-order events (watermarks), and a batch reconciliation layer (lambda) for correctness.",
      "Real-time 'clicks per campaign per minute' via Flink windows, reconciled nightly by a Spark batch job for billing accuracy.",
      "Ad tech, analytics counters, real-time dashboards, usage-based billing.",
      "Clicks -> Kafka -> [Flink windowed aggregation] -> OLAP store -> dashboards ; batch reconcile (lambda)",
      "Exactly-once vs at-least-once; late events; dedup at scale; hot keys (popular ads); accuracy for billing.",
      "Flink/Spark Streaming; Kafka; lambda architecture; OLAP (Druid/Pinot/ClickHouse).",
      ["ad-click", "aggregation", "streaming", "flink", "exactly-once", "windowing", "lambda-architecture"]),
    E("classic", "Design a Hotel Reservation System",
      "Book rooms without overbooking under concurrency, with search, availability, and payment.",
      "Inventory of room-type/date availability with strong consistency on the reservation write (transaction/conditional update or reserve-with-TTL). Search service over availability (cached); a reservation service holds a room during checkout, confirms on payment (saga), and releases on timeout. Idempotency prevents double-booking on retries.",
      "Booking.com: search availability (cached) -> hold room (TTL) -> pay -> confirm; concurrent holds prevented by an atomic decrement.",
      "Hotels, flights, event tickets, restaurant reservations, appointment booking.",
      "Search(avail cache) -> hold room (TTL, atomic) -> pay (saga) -> confirm / release on timeout",
      "Overbooking under concurrency; hold expiry; distributed transaction across pay+book; search freshness.",
      "SQL transactions/row locks vs reserve-with-TTL; saga; idempotency keys; vs eventual-consistency (risky here).",
      ["reservation", "booking", "concurrency", "overbooking", "saga", "inventory"]),
    E("classic", "Design a Proximity Service (Yelp / Nearby)",
      "Find businesses/objects within a radius of a location, fast, at scale.",
      "Index locations with a geospatial scheme: geohash (encode lat/long into a prefix string — nearby points share prefixes) or a quadtree/S2 cells. Query = find the cell(s) covering the radius and scan candidates, then filter by exact distance. Store business data in a DB; cache hot regions.",
      "Yelp: 'restaurants near me' resolves your geohash cell + neighbors, fetches candidates, ranks by distance/rating.",
      "Local search, ride-hailing driver matching, store locators, geofencing.",
      "Locations -> [geohash / quadtree / S2 index] ; query(lat,long,radius) -> cover cells -> candidates -> distance filter",
      "Cell-boundary edge cases; dense-area hotspots; dynamic (moving) objects; radius vs cell granularity.",
      "Geohash, quadtree, Google S2, Uber H3; PostGIS, Redis Geo, Elasticsearch geo.",
      ["proximity", "geospatial", "geohash", "quadtree", "s2", "location", "nearby"]),
    E("classic", "Design Google Maps",
      "Provide maps, geocoding, and route/ETA computation over a planet-scale road graph.",
      "Store the road network as a graph (nodes=intersections, edges=roads with weights). Routing uses shortest-path (Dijkstra/A*) with precomputed contraction hierarchies for speed at scale; partition the map into regions/tiles served from a CDN. ETA blends edge weights with live traffic (from aggregated GPS). Geocoding maps addresses<->coordinates.",
      "Google/Apple Maps: precomputed hierarchies make cross-country routing fast; live traffic adjusts ETAs; map tiles served from CDN.",
      "Navigation, ride-hailing routing, logistics/last-mile, geocoding.",
      "Road graph -> [contraction hierarchies] ; route: A*/Dijkstra + live traffic -> ETA ; tiles via CDN",
      "Graph size/precompute cost; live-traffic freshness; dynamic rerouting; map data updates.",
      "Dijkstra/A*, contraction hierarchies, OSRM/Valhalla; tile servers; S2 for spatial.",
      ["maps", "routing", "shortest-path", "dijkstra", "graph", "geospatial", "eta"]),
    E("classic", "Design a Real-time Gaming Leaderboard",
      "Show top-N players and a user's rank over millions of players, updated in real time.",
      "Use a Redis sorted set (score->member) — O(log n) updates and O(log n + k) top-k/rank queries. Shard by game/season; for huge scale, approximate ranks or bucket by score. Persist to a DB for durability; recompute/rebuild on failover.",
      "A battle-royale leaderboard: ZADD updates a player's score; ZREVRANGE returns the top 100; ZREVRANK gives 'your rank'.",
      "Games, contests, sales leaderboards, trending rankings.",
      "Score update -> Redis SortedSet (ZADD) ; top-N -> ZREVRANGE ; my rank -> ZREVRANK",
      "Memory at scale; exact rank for tens of millions is costly (approximate/bucket); persistence + rebuild.",
      "Redis sorted sets; approximate ranking; sharded leaderboards; DB-backed rebuild.",
      ["leaderboard", "redis", "sorted-set", "ranking", "real-time", "gaming"]),
    E("classic", "Design a Digital Wallet / Payment Ledger",
      "Move money between accounts with strict correctness (no lost/duplicated funds) and a full audit trail.",
      "Model money as an immutable, append-only double-entry ledger (every transfer = balanced debit + credit). Ensure atomicity across accounts (transaction, or saga + idempotency for cross-service); derive balances from the ledger (or a reconciled snapshot). Idempotency keys stop double-spend on retries; reconciliation catches drift.",
      "A wallet transfer writes a balanced ledger entry (debit A, credit B) in one transaction; balances are projections; every op is auditable.",
      "Digital wallets, payments, banking, in-game currency, marketplace payouts.",
      "Transfer(key) -> atomic double-entry ledger (debit + credit) -> balance projection ; reconcile",
      "Strong consistency limits throughput; cross-service atomicity (saga); reconciliation; regulatory/audit.",
      "Double-entry ledger; SQL ACID vs saga+idempotency; event sourcing; TigerBeetle (purpose-built).",
      ["wallet", "payment", "ledger", "double-entry", "idempotency", "consistency", "audit"]),
    E("classic", "Design a Stock Exchange / Matching Engine",
      "Match buy/sell orders fairly with ultra-low latency and strict correctness at very high throughput.",
      "A matching engine keeps an in-memory order book per symbol (price-time priority); incoming orders match against the best opposite price or rest in the book. Deterministic, single-threaded per symbol for correctness; sequenced input (a total order via a sequencer) enables replay/recovery; downstream feeds market data + settlement. Latency is measured in microseconds.",
      "Orders -> sequencer -> matching engine (order book, price-time) -> trades -> market-data feed + clearing/settlement.",
      "Exchanges, crypto trading, ad exchanges, any auction/matching system.",
      "Orders -> [sequencer] -> matching engine (in-mem order book, price-time) -> trades -> market data + settlement",
      "Extreme latency/determinism needs; fairness; recovery via event replay; regulatory; hot-path can't block.",
      "In-memory order book, event sourcing/sequencer (LMAX Disruptor), FIX protocol; vs DB-backed (too slow).",
      ["stock-exchange", "matching-engine", "order-book", "low-latency", "event-sourcing", "trading"]),
    E("classic", "Design a Rate Limiter (interview)",
      "Cap requests per client to protect services and enforce quotas, accurately, in a distributed setting.",
      "Pick an algorithm — token bucket (allows bursts), sliding-window log/counter (accurate), fixed window (simple, boundary spikes), leaky bucket (smooth). Keep counters in a central store (Redis) keyed by client+window with atomic ops/Lua for correctness across nodes. Return 429 + Retry-After; place at the gateway.",
      "API gateway limits each API key to 1000 req/min via a Redis sliding-window counter, returning 429 when exceeded.",
      "API gateways, login/OTP, abuse/bot defense, fair-use quotas, flash-sale fairness.",
      "Request -> [Redis counter per client+window (atomic/Lua)] -> allow or 429 Retry-After",
      "Distributed accuracy (races, clock skew); memory for sliding-window logs; false positives behind NAT.",
      "Token bucket vs sliding window vs leaky bucket; Redis; gateway (Kong/Envoy); vs client-side.",
      ["rate-limiter", "token-bucket", "sliding-window", "redis", "throttling", "gateway"],
      detail=_DETAIL_RL),
    E("classic", "Design a Distributed Cache",
      "Serve reads at microsecond latency and take load off databases, across a fleet, larger than one machine's RAM.",
      "Partition keys across cache nodes with consistent hashing (minimal remap on scaling); replicate hot shards; pick an eviction policy (LRU/LFU/TTL) and a write policy (write-through/back/around). Handle cache-miss stampede with request coalescing + jittered TTLs; keep it consistent with the DB via TTL + event-driven invalidation.",
      "A Redis Cluster fronts the product DB: reads hit cache, misses populate it, and price-change events invalidate keys.",
      "Read-heavy services, sessions, computed results, database offload.",
      "Client -> consistent-hash -> {cache node shards (LRU, replicated)} -> miss -> DB -> populate",
      "Invalidation/consistency; hot keys; stampede on expiry; cost of RAM; rebalancing on node change.",
      "Redis Cluster, Memcached, Hazelcast; consistent hashing; write-through vs write-back; near-cache.",
      ["distributed-cache", "consistent-hashing", "lru", "cache-invalidation", "stampede", "redis"]),
    E("classic", "Design a Notification Service",
      "Deliver billions of multi-channel notifications (push/SMS/email/in-app) reliably, with preferences and de-dup.",
      "Event-driven: producers publish notification events -> a service resolves recipients + channel prefs + templates -> enqueues per-channel jobs -> workers dispatch to providers with retries, rate limits, and delivery tracking. Priority queues separate transactional from marketing; idempotency/dedup prevents double-sends; opt-outs are enforced centrally.",
      "OrderShipped event -> resolve user prefs -> render template -> queue -> worker -> APNs/FCM/SES/Twilio, with retry + tracking.",
      "Transactional alerts, OTP, marketing campaigns, digests, in-app inboxes.",
      "Events -> Notification svc (prefs+templates) -> per-channel [Queue] -> workers -> providers (retry/track)",
      "Provider limits/failures; deliverability/spam; ordering; dedup; rate limits; preference management.",
      "SNS/SES, Twilio, FCM/APNs, SendGrid; Kafka/SQS; template engines; per-user preference store.",
      ["notification", "push", "sms", "email", "fan-out", "queue", "preferences", "idempotency"]),
    E("classic", "Design a Payment Reconciliation System",
      "Guarantee that internal ledgers, the payment gateway, and the bank all agree — catching missing, duplicate, or mismatched transactions.",
      "Ingest records from all sources (internal ledger, PSP/gateway settlement files, bank statements) into a common schema, then run a matching engine (by transaction/order ref, amount, date) to classify each as matched / missing / duplicate / amount-mismatch. Exceptions go to a review queue + auto-rules; run as a scheduled batch (e.g., daily) with an immutable audit trail. Idempotency keys upstream reduce duplicates.",
      "A nightly job matches Stripe settlement, the internal ledger, and bank credits; unmatched items open exceptions for finance.",
      "Payments, marketplaces/payouts, banking, subscription billing, finance close.",
      "Ledger + PSP file + bank statement -> normalize -> [matching engine] -> matched / exceptions -> review + audit",
      "Timing differences (T+n settlement); fees/FX splitting one txn into many; huge volumes; exception backlog.",
      "Batch (Spark/SQL) matching, rules engines, double-entry ledger; TigerBeetle; specialized recon tools.",
      ["reconciliation", "payments", "ledger", "matching", "batch", "audit", "finance"]),
    E("classic", "Design an S3-like Object Storage",
      "Store exabytes of immutable objects durably (11 nines), cheaply, and serve them over HTTP at massive scale.",
      "A metadata service maps object keys (bucket/key) to the physical locations of their data chunks; a data layer stores chunks across many nodes/disks with replication or erasure coding for durability. A stateless API layer handles PUT/GET/multipart; background processes handle repair, rebalancing, and lifecycle (tiering to cold storage). Consistent hashing/placement spreads load; a CDN fronts hot reads.",
      "S3: PUT splits a large object into parts stored with erasure coding across racks; GET streams them back; metadata tracks the manifest.",
      "Media/backups, data lakes, static hosting, any large blob storage.",
      "API (PUT/GET) -> Metadata svc (key->chunks) -> Data nodes (replication/erasure code) ; repair + tiering; CDN reads",
      "Metadata scale; durability vs cost (replication vs erasure coding); eventual consistency (historically); large-object handling.",
      "AWS S3, GCS, Azure Blob, MinIO, Ceph; erasure coding (Reed-Solomon) vs 3x replication.",
      ["object-storage", "s3", "erasure-coding", "durability", "blob", "metadata", "chunking"]),
    E("classic", "Design a Distributed Job Scheduler",
      "Run millions of scheduled/recurring/one-off jobs reliably, exactly-once-ish, across a worker fleet, surviving failures.",
      "A scheduler persists jobs + next-run times (a DB or time-bucketed store); a dispatcher polls due jobs and enqueues them to a queue; workers pull, execute, and report status with retries + backoff and idempotency (so a re-delivered job doesn't double-run). Use leader election for the scheduler, visibility timeouts for at-least-once, and dead-letter queues for poison jobs.",
      "A cron-at-scale service enqueues due jobs to SQS; workers process with retries; a leader ensures a job is scheduled once.",
      "Cron at scale, workflow steps, retries/backoff, delayed tasks, batch orchestration.",
      "Jobs (next-run) -> dispatcher (poll due) -> [Queue] -> workers (execute, retry, idempotent) -> status/DLQ",
      "Exactly-once is hard (favor at-least-once + idempotency); clock/timezone/DST; thundering herd at round times; long jobs.",
      "Quartz, Airflow/Dagster, Temporal, K8s CronJobs, SQS delay + workers; leader election (ZK/etcd).",
      ["job-scheduler", "cron", "queue", "idempotency", "retry", "leader-election", "workflow"]),
]

# Fill tags: use explicit ones where given, else derive from title + category,
# and always include the category's broad tag(s).
for _e in ENTRIES:
    _base = _CATEGORY_TAGS.get(_e["cat"], [])
    if _e.get("tags"):
        _e["tags"] = sorted(set([t.lower() for t in _e["tags"]] + _base))
    else:
        _e["tags"] = sorted(set(_title_tags(_e["title"]) + _base))
